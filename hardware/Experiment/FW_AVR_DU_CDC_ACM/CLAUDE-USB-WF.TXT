Analyze the source codes in:
    /home/aloysius/Projects/JxMake/hardware/Experiment/FW_AVR_DU_CDC_ACM

Do not analyze or modify the build system files:
    JxMakeFile
    jxmake

Manufacturer SDK files included in scope:
    MCC_AVRDU/system/src/protected_io.S
    MCC_AVRDU/system/src/syscfg.c
    MCC_AVRDU/usb/usb_descriptors.c
    MCC_AVRDU/usb/usb_device.c
    MCC_AVRDU/usb/src/usb0.c
    MCC_AVRDU/usb/usb_cdc/circular_buffer/circular_buffer.c
    MCC_AVRDU/usb/usb_cdc/usb_cdc.c
    MCC_AVRDU/usb/usb_cdc/usb_cdc_virtual_serial_port.c
    MCC_AVRDU/usb/usb_common/usb_core.c
    MCC_AVRDU/usb/usb_common/usb_core_descriptors.c
    MCC_AVRDU/usb/usb_common/usb_core_events.c
    MCC_AVRDU/usb/usb_common/usb_core_requests.c
    MCC_AVRDU/usb/usb_common/usb_core_requests_device.c
    MCC_AVRDU/usb/usb_common/usb_core_requests_endpoint.c
    MCC_AVRDU/usb/usb_common/usb_core_requests_interface.c
    MCC_AVRDU/usb/usb_common/usb_core_transfer.c
    MCC_AVRDU/usb/usb_peripheral/usb_peripheral.c
    MCC_AVRDU/usb/usb_peripheral/usb_peripheral_endpoint.c
    MCC_AVRDU/usb/usb_peripheral/usb_peripheral_read_write.c

Do not analyze other manufacturer SDK files unless they are directly included from the above.

The project overall goal:
    Strip down the manufacturer SDK and USB CDC‑ACM stack to the minimum required functionality, reducing code‑bloat and improving optimization (size and performance).
    The CDC‑ACM stack must remain configurable (single, dual, etc.) and must preserve handling of control signals such as SetStopBits, USB_CDCSetBaud, LINE_CODING, etc.

Workflow:
1. Analyze and propose changes only within the specified directory.
2. After each change, run:
       ./jxmake build
   - If build succeeds, pause and wait for me to upload/test the firmware.
   - If build fails, stop and annotate suspected causes in comments above the relevant lines; do not attempt fixes until instructed.
3. Do not strip down to raw register/SFR access yet. First flatten to the lowest‑level manufacturer SDK functions.
4. If a manufacturer SDK file is no longer needed, propose its removal. Do not run `rm` until I confirm.
5. Proceed only after I confirm runtime behavior is intact.



The overall goal is to strip down the manufacturer SDK and USB CDC‑ACM stack to the minimum required functionality, reducing code‑bloat and improving optimization (size and performance). The CDC‑ACM stack must remain configurable (single, dual, etc.) and must preserve handling of control signals such as SetStopBits, USB_CDCSetBaud, LINE_CODING, etc.





Do not forget to update:
tar -cjvpf ../../../0_excluded_directory/_0_claude-mem-20260518.tar.gz ~/.claude/ ~/.claude.json



firmware intact, continue
firmware intact, commit and continue
firmware intact, commit and stop for now; we'll continue later
firmware intact, but usage quota is almost up; commit and we'll continue later



Act as a precise source-code transformation engine for all .h and .c files in this directory.
Rules:
1. Convert all Doxygen-style comments (/** */, ///, //!<) into normal comments:
   - /** ... */ → /* ... */
   - /// → //
   - //!< → //
2. Remove all Doxygen tags (@ingroup, @brief, @param, @return, @def, etc.).
   - Keep only the descriptive text following them.
   - Indent parameter descriptions by 4 spaces for readability.
   - Replace "@return" with "return" (no @).
3. Preserve indentation, spacing, and brace placement exactly as in the original code.
4. Do NOT alter code outside comments.
5. Output ONLY raw source code. No markdown, no explanations.
