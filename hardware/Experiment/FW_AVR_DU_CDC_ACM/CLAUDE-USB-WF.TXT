Analyze the source codes in:
    /home/aloysius/Projects/JxMake/hardware/Experiment/FW_AVR_DU_CDC_ACM/MCC_AVRDU_USB_CDC_ACM

Project goal:
    Strip down the manufacturer USB CDC‑ACM stack to the minimum required functionality, reducing code‑bloat and improving optimization (size and performance).
    The CDC‑ACM stack must remain configurable (single, dual, etc.) and must preserve handling of control signals such as SetStopBits, USB_CDCSetBaud, LINE_CODING, etc.

Workflow:
1. Refer to:
       /home/aloysius/Projects/JxMake/hardware/Experiment/FW_AVR_DU_CDC_ACM/FCG.txt
   for the already analyzed callgraph (ignore functions outside the USB stack).
   Notes in FCG.txt may advise not to inline callees into ISRs; however, you may inline a callee if it is small or not referenced elsewhere.
2. Analyze and propose changes only within the specified directory.
3. After each change, run:
       ./jxmake build
   - If the build succeeds, pause and wait for me to upload/test the firmware. Proceed to the next run only after I confirm runtime behavior is intact.
   - If the build fails, revert the previous changes and add annotations so the same changes will not be attempted again.
   - Use annotations (e.g., DONE!, FAILED!, etc.) in FCG.txt strictly to mark progress.
4. Do not strip down to raw register/SFR access yet. First flatten to the lowest‑level manufacturer USB CDC‑ACM functions.
5. If a manufacturer USB CDC‑ACM file is no longer needed or becomes empty, propose its removal. Do not run `rm` until I confirm.
6. Example of a failed change from a previous attempt:
   - usb_core_descriptors.c : if NEW_CODE is defined, the firmware broke; see 'NOTE @Claude' inside for more details.
7. Example of a successful change from a previous attempt:
   - usb_core_requests_device.c : see 'NOTE @Claude' inside for more details.





Do not forget to update:
tar -cjvpf ../../../0_excluded_directory/_0_claude-mem-20260518.tar.gz ~/.claude/ ~/.claude.json



firmware intact, continue
firmware intact, commit and continue
firmware intact, commit and stop for now; we'll continue later
firmware intact, but usage quota is almost up; commit and we'll continue later



Act as a precise source-code transformation engine for all .h and .c files in this directory.
Rules:
1. Convert all Doxygen-style comments (/** */, ///, //!<) into normal comments:
   - /** ... */ → /* ... */
   - /// → //
   - //!< → //
2. Remove all Doxygen tags (@ingroup, @brief, @param, @return, @def, etc.).
   - Keep only the descriptive text following them.
   - Indent parameter descriptions by 4 spaces for readability.
   - Replace "@return" with "return" (no @).
3. Preserve indentation, spacing, and brace placement exactly as in the original code.
4. Do NOT alter code outside comments.
5. Output ONLY raw source code. No markdown, no explanations.






Act as a precise source-code transformation engine.
Scope:
- Browse the current working directory and its sub directory.
- Process all .c and .h files.

Rules:
1. Locate all multiline comments (/* ... */).
2. Remove any lines that contain only:
   - A bare file name (any .c or .h file present in the project).
   - A bare identifier that matches another file stem (e.g., usb_core_requests, usb_peripheral_avr_du).
   - A bare type or struct name repeated redundantly (e.g., USB_DESCRIPTOR_PTR_t, USB_ENDPOINT_TABLE_struct).
3. Keep descriptive sentences, parameter explanations, and meaningful documentation.
4. Preserve indentation, spacing, and brace placement exactly as in the original code.
5. Do NOT alter code outside comments.
6. Output ONLY raw source code. No markdown, no explanations.

Confirmation:
- For each file, generate a unified diff showing proposed deletions.
- Ask for one confirmation before applying all changes in that file.
- Do not ask for confirmation per line.

Constraints:
- Single pass over all files.
- Spaces remain spaces, tabs remain tabs.
- Do not introduce new formatting conventions.

