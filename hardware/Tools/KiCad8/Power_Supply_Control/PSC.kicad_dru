(version 1)

(rule limit_default_width
  (condition "A.Type == 'Track' && A.NetClass == 'Default'")
  (constraint track_width (max 0.3mm))
)

(rule enforce_power_width
  (condition "A.Type == 'Track' && A.NetClass == 'Power'")
  (constraint track_width (min 0.5mm))
)
