====================================================================================================
WindowsDriverInstaller_FFM.java - Win32 API / Native Structure Reference
====================================================================================================

Internal engineering reference. Not a Microsoft document and not a substitute for the Windows SDK.
It exists to help a future maintainer of this class read the FFM (Java Foreign Function & Memory
API) bindings without having to re-derive every struct offset and calling convention from scratch.

Everything below describes what THIS FILE actually does - the field layouts, flag values, and
calling sequences match the static block and the various `_elevated*`/`_signCatalog` helper methods
in WindowsDriverInstaller_FFM.java as of the version this document was written against. Where a
detail was confirmed against Microsoft's own reference pages, it is marked VERIFIED with a Source:
line. Where something instead has to be reconstructed because Microsoft does not document it, it is
marked RECONSTRUCTED / INFERRED, and the reasoning is spelled out so a future reader can judge how
much to trust it.

Some Source: URLs below (Microsoft Learn API reference pages in particular) may prompt for Microsoft
account sign-in when opened directly in a browser, even though the underlying page is public
reference documentation. This does not affect how those facts were verified - it only means a reader
following the link by hand may hit a sign-in prompt.

No third-party source code is reproduced or excerpted anywhere in this file.

FFM conventions used throughout this file:

  DW  = ValueLayout.JAVA_INT : used for DWORD, BOOL, LONG, HRESULT, ALG_ID - all 32-bit on Win32,
                               regardless of the process being x64; Win32 kept these types 32-bit
                               for ABI compatibility reasons unrelated to pointer width

  PTR = ValueLayout.ADDRESS  : used for every HANDLE, HWND, HKEY, HCERTSTORE, LP*, or
                               output-by-reference parameter

All downcalls are bound through the private `_bind(linker, lib, name, fd)` helper, which does
`linker.downcallHandle(lib.find(name).orElseThrow(...), fd)` - i.e. a plain default-convention
downcall handle for each symbol, no critical/trivial linker options are used.

DISCLAIMER:
    Nothing in this file is copied or paraphrased from Microsoft's text - every description below is
    written from scratch, using the SDK reference pages only to check facts (function/struct member
    existence, order, size, and behavior), not as source text. All structure layouts, field offsets,
    sizes, and alignments described here were independently derived and/or verified against public
    Windows SDK headers and Microsoft Learn documentation.


====================================================================================================
SECTION A - Process / Elevation Layer (Kernel32.dll, Shell32.dll)
====================================================================================================

This layer exists because two operations later in the file (certificate creation/trust and catalog
signing) need an elevated token, and the only way this FFM backend can get one is to relaunch its
own JVM through ShellExecuteExW with lpVerb="runas" and wait on the resulting process handle.

CreateFileW/CloseHandle appear again in Section D (used to open the INF file for hashing), not just
here.

----------------------------------------------------------------------------------------------------
CreateFileW
----------------------------------------------------------------------------------------------------
Source             : https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew

DLL                : Kernel32.dll
Header             : fileapi.h
Calling convention : __stdcall (all Win32 API calls in this document use the standard Win32 calling
                     convention; the FFM Linker's default downcall convention on Windows already
                     matches this, so no explicit convention option is passed)

Parameters         : lpFileName (LPCWSTR), dwDesiredAccess (DWORD), dwShareMode (DWORD),
                     lpSecurityAttributes (LPSECURITY_ATTRIBUTES, optional), dwCreationDisposition
                     (DWORD), dwFlagsAndAttributes (DWORD), hTemplateFile (HANDLE, optional)
Return value       : HANDLE to the opened file, or INVALID_HANDLE_VALUE (-1 as a pointer-sized
                     value) on failure.
Purpose            : General-purpose file/device open call. This class uses only the
                     read-a-file-that-already-exists path.

FFM notes          : `FunctionDescriptor.of(PTR, PTR, DW, DW, PTR, DW, DW, PTR)` - 7 parameters,
                     matches the prototype above 1:1. Used once, in `_elevatedCreateAndSignCatalog`,
                     to open the INF file for GENERIC_READ/FILE_SHARE_READ/OPEN_EXISTING so its
                     bytes can be hashed via CryptCATAdminCalcHashFromFileHandle2. The returned
                     handle is compared against -1L (INVALID_HANDLE_VALUE) via `hInfFile.address()
                     == INVALID_HANDLE_VALUE`, and is always closed in a `finally` block.

----------------------------------------------------------------------------------------------------
CloseHandle
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/handleapi/nf-handleapi-closehandle

DLL          : Kernel32.dll
Header       : handleapi.h

Parameters   : hObject (HANDLE)
Return value : BOOL (nonzero on success)
Purpose      : Releases any Win32 kernel handle (file, process, etc.).

FFM notes    : `FunctionDescriptor.of(DW, PTR)`. Called on both the INF file handle (Section D) and
               the elevated child process handle (`_shellExecuteElevatedAndWait`), each time from a
               `finally` block.

----------------------------------------------------------------------------------------------------
GetLastError
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/errhandlingapi/nf-errhandlingapi-getlasterror

DLL          : Kernel32.dll
Header       : errhandlingapi.h

Parameters   : none
Return value : DWORD, the calling thread's last-error code.
Purpose      : Standard Win32 extended-error retrieval, read immediately after a Win32 call reports
               failure.

FFM notes    : `FunctionDescriptor.of(DW)`. Wrapped by the private `_lastError()` helper (`(int)
               _GetLastError.invoke()`), called from nearly every failure branch in this file to
               enrich the returned log/message. Caveat worth flagging for a future maintainer:
               GetLastError's value is only meaningful immediately after the failing call, on the
               same thread, before any other Win32 call runs - the code here is careful to call it
               right after each checked API, which is correct, but any future refactor that reorders
               these calls (e.g. moving logging after other cleanup calls) would silently corrupt
               the reported error code.

----------------------------------------------------------------------------------------------------
WaitForSingleObject
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/synchapi/nf-synchapi-waitforsingleobject

DLL          : Kernel32.dll
Header       : synchapi.h

Parameters   : hHandle (HANDLE), dwMilliseconds (DWORD)
Return value : DWORD - WAIT_OBJECT_0 (0) on signal, WAIT_TIMEOUT (0x102) on timeout, WAIT_FAILED on
               error.
Purpose      : Blocks the calling thread until the given kernel object (here, always a process
               handle) becomes signaled or the timeout elapses.

FFM notes    : `FunctionDescriptor.of(DW, PTR, DW)`. Used in `_shellExecuteElevatedAndWait` to wait
               for the elevated child process, with the timeout computed as `waitTimeMinutes *
               60_000`. The returned code is compared against the local constant `WAIT_TIMEOUT =
               0x102`; on timeout the code calls TerminateProcess and reports RETCODE_TIMEOUT.

----------------------------------------------------------------------------------------------------
GetExitCodeProcess
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-getexitcodeprocess

DLL          : Kernel32.dll
Header       : processthreadsapi.h

Parameters   : hProcess (HANDLE), lpExitCode (LPDWORD, out)
Return value : BOOL
Purpose      : Reads back the exit code of a (by then already-signaled) process.

FFM notes    : `FunctionDescriptor.of(DW, PTR, PTR)`. Called right after WaitForSingleObject returns
               non-timeout; the exit code buffer is a 4-byte arena allocation read back with
               `exitCodeOut.get(ValueLayout.JAVA_INT, 0)`. This exit code becomes the returned
               XCom.Pair's first element, i.e. the elevated child's own `System.exit(exitCode)`
               value from `main()` flows all the way back to the un-elevated caller through this
               path.

----------------------------------------------------------------------------------------------------
TerminateProcess
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-terminateprocess

DLL          : Kernel32.dll
Header       : processthreadsapi.h

Parameters   : hProcess (HANDLE), uExitCode (UINT)
Return value : BOOL
Purpose      : Forcibly ends a process; here used only as a timeout backstop.

FFM notes    : `FunctionDescriptor.of(DW, PTR, DW)`. Only reached on the WAIT_TIMEOUT branch, called
               with exit code 1. There is no follow-up wait after calling this - the code proceeds
               directly to returning RETCODE_TIMEOUT, so termination is best-effort/fire-and-forget
               rather than confirmed.

----------------------------------------------------------------------------------------------------
LocalFree
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-localfree

DLL          : Kernel32.dll
Header       : winbase.h

Parameters   : hMem (HLOCAL)
Return value : HLOCAL (NULL on success, unlike most BOOL-returning Win32 calls)
Purpose      : Frees memory that Crypt32 allocated on the caller's behalf via the
               CRYPT_ENCODE_ALLOC_FLAG convention (see CryptEncodeObjectEx below).

FFM notes    : `FunctionDescriptor.of(PTR, PTR)` - return type is PTR (not DW) because LocalFree
               returns a handle, not a BOOL; the return value is discarded at every call site in
               this file. Called on `ekuEncoded` (Section B) and `spcLinkEnc` (Section D), each in a
               `finally` block matching the CryptEncodeObjectEx call that produced it. Ownership
               rule: any buffer that CryptEncodeObjectEx returned via its out-pointer with
               CRYPT_ENCODE_ALLOC_FLAG set was allocated by Crypt32's internal allocator and must be
               released with LocalFree, never freed by any other means and never left for the arena
               to reclaim (the arena did not allocate it).

----------------------------------------------------------------------------------------------------
ShellExecuteExW
----------------------------------------------------------------------------------------------------
Source             : https://learn.microsoft.com/en-us/windows/win32/api/shellapi/nf-shellapi-shellexecuteexw

DLL                : Shell32.dll
Header             : shellapi.h
Calling convention : __stdcall

Parameters         : pExecInfo (SHELLEXECUTEINFOW *, in/out - the struct is both filled in by the
                     caller and partially filled in by the function, notably hProcess and hInstApp)
Return value       : BOOL (TRUE on success; call GetLastError on FALSE)
Purpose            : Launches a file/verb combination through the shell rather than through
                     CreateProcess directly. This is what makes UAC elevation possible from user
                     code: passing lpVerb="runas" tells the shell to invoke the "Run as
                     administrator" activation path (an elevated COM moniker under the hood), which
                     CreateProcess itself cannot do.

FFM notes          : `FunctionDescriptor.of(DW, PTR)` - single parameter, the struct pointer. Called
                     exactly once per elevated operation, from `_shellExecuteElevatedAndWait`.
                     GetLastError()==1223 (ERROR_CANCELLED) after a FALSE return is treated
                     specially and mapped to RETCODE_UAC_DECLINED, distinguishing "user clicked No
                     on the UAC prompt" from every other failure mode.

Windows-specific caveats relevant to this implementation:
    - Because SEE_MASK_NOCLOSEPROCESS is set, the returned hProcess handle is the caller's
      responsibility to close (done in a `finally` in `_shellExecuteElevatedAndWait`).
    - lpDirectory is explicitly set to the parent's `user.dir` because an elevated child's working
      directory is not guaranteed to match the parent's; this matters here because the launched
      command line embeds a (resolved-to-absolute, per `_absoluteClassPath()`) classpath, but if any
      other relative path were ever added to the command line it would resolve against lpDirectory,
      not the parent's cwd, unless lpDirectory is set correctly - which this code already does.
    - Microsoft's own documentation notes COM should be initialized before calling ShellExecuteEx
      for certain shell extensions; this code does not call CoInitializeEx before invoking it. This
      has not caused an observed failure with the "runas" verb specifically (which does not go
      through arbitrary shell extensions), but it is a latent gap relative to Microsoft's documented
      best practice if the verb or target ever changes.

----------------------------------------------------------------------------------------------------
STRUCTURE: SHELLEXECUTEINFOW
----------------------------------------------------------------------------------------------------
Source             : https://learn.microsoft.com/en-us/windows/win32/api/shellapi/ns-shellapi-shellexecuteinfow
                     (struct member list cross-checked against the ShellExecuteExW page above; also
                     generally well-known, stable Win32 struct).

Fields             : Field byte offsets, as hardcoded in the SEI_* constants in this file:
                         cbSize(0,DWORD) fMask(4,DWORD) hwnd(8,HWND) lpVerb(16,LPCWSTR)
                         lpFile(24,LPCWSTR) lpParameters(32,LPCWSTR) lpDirectory(40,LPCWSTR)
                         nShow(48,int) hInstApp(56,HINSTANCE) lpIDList(64,void*) lpClass(72,LPCWSTR)
                         hkeyClass(80,HKEY) dwHotKey(88,DWORD) hIcon/hMonitor(96,HANDLE)
                         hProcess(104,HANDLE)
Size               : 112 bytes, 8-byte aligned (x64).
Purpose            : Describes a shell "verb" invocation (e.g. "open", "runas") plus the
                     file/parameters/window-state to launch it with, and receives back a process
                     handle and a pseudo-HINSTANCE result code.

FFM notes          : Built by hand in `_shellExecuteElevatedAndWait` via `arena.allocate(SEI_SIZE,
                     8)` and explicit `.set(...)` calls at each SEI_* offset - there is no reusable
                     "StructLayout" object, just raw offsets, which is why the comment block above
                     the SEI_* constants exists as the canonical description of the layout. fMask is
                     set to SEE_MASK_NOCLOSEPROCESS(0x40) | SEE_MASK_NOASYNC(0x100); nShow is
                     SW_HIDE(0); lpVerb is always the literal "runas".

Ownership/lifetime : All the LPCWSTR fields point into the same `Arena .ofConfined()` used for the
                     rest of the call, so they stay valid for the whole synchronous ShellExecuteExW
                     + WaitForSingleObject + GetExitCodeProcess sequence and are freed automatically
                     when the try-with-resources arena closes - no manual cleanup needed for the
                     strings themselves. hProcess, once received back into the struct, is a real
                     kernel handle and IS the caller's responsibility (see CloseHandle above) - it
                     is not implicitly cleaned up by the arena.


====================================================================================================
SECTION B - Certificate Store / Self-Signed Certificate Layer (Crypt32.dll)
====================================================================================================

This section covers everything used to check for/create/trust the self-signed code-signing
certificate this tool generates. Two operating system certificate stores are relevant throughout:
the per-machine "Root"/"TrustedPublisher" stores (which is what actually makes Windows trust a
driver signed with this certificate) and the per-user "My" store (which is where the certificate's
private-key linkage lives so it can be used later to sign the catalog).

----------------------------------------------------------------------------------------------------
CertOpenStore
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certopenstore

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : lpszStoreProvider, dwMsgAndCertEncodingType (DWORD), hCryptProv (optional, unused
               here -> 0), dwFlags (DWORD), pvPara (provider-specific, here a store name)
Return value : HCERTSTORE, or NULL on failure.
Purpose      : Opens (or implicitly creates) a certificate store of a given provider type at a given
               scope (machine vs. user) and name.

FFM notes    : `FunctionDescriptor.of(PTR, PTR, DW, PTR, DW, PTR)`. The first parameter
               (lpszStoreProvider) is passed as `_handleOf(CERT_STORE_PROV_SYSTEM_W)` where
               CERT_STORE_PROV_SYSTEM_W = 10L - this is one of the "predefined, small-integer store
               provider" values that wincrypt.h expects to be passed by value in the pointer-typed
               parameter slot rather than as a real string pointer (a well-known, if unusual, Win32
               ABI convention also used for several other "or provide LPCSTR" parameters in this API
               family). Wrapped by the private `_certStoreOpen(arena, locationFlag, storeName)`
               helper used throughout the rest of this file. Called with
               CERT_SYSTEM_STORE_LOCAL_MACHINE for "Root"/"TrustedPublisher" and
               CERT_SYSTEM_STORE_CURRENT_USER for "My".

----------------------------------------------------------------------------------------------------
CertCloseStore
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certclosestore

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : hCertStore (HCERTSTORE), dwFlags (DWORD - always 0 here)
Return value : BOOL
Purpose      : Releases a store handle obtained from CertOpenStore.

FFM notes    : `FunctionDescriptor.of(DW, PTR, DW)`. Always called with flags=0, matched 1:1 with
               every successful CertOpenStore call in this file, generally from `finally`.

----------------------------------------------------------------------------------------------------
CertFindCertificateInStore
----------------------------------------------------------------------------------------------------
Source             : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certfindcertificateinstore

DLL                : Crypt32.dll
Header             : wincrypt.h

Parameters         : hCertStore (HCERTSTORE), dwCertEncodingType (DWORD), dwFindFlags (DWORD, always
                     0 here), dwFindType (DWORD), pvFindPara (find-type-specific), pPrevCertContext
                     (optional, always NULL here - meaning "start search from the beginning")
Return value       : PCCERT_CONTEXT matching the search, or NULL if none found.
Purpose            : Iterative certificate-store search primitive; used here in its simplest form
                     (single call, no continuation) to check whether a certificate with a given
                     subject substring already exists.

FFM notes          : `FunctionDescriptor.of(PTR, PTR, DW, DW, DW, PTR, PTR)` - 7 slots: 1 return
                     type (PTR/PCCERT_CONTEXT) followed by 6 argument layouts, one per parameter
                     listed above. dwFindType is always the module constant CERT_FIND_SUBJECT_STR_W
                     = (8 << 16) | 7 (the standard encoding of CERT_COMPARE_NAME_STR_W shifted by
                     CERT_COMPARE_SHIFT, OR'd with CERT_INFO_SUBJECT_FLAG - a well-documented but
                     easy-to-miscount bit pattern, worth double-checking against wincrypt.h if this
                     value is ever touched). Used in `isProviderAlreadyTrusted` (search by provider
                     name across "Root"/"TrustedPublisher") and in `_elevatedCreateAndSignCatalog`
                     (locate the signing cert in CurrentUser\My by provider name before calling
                     `_signCatalog`).

Ownership/lifetime : every non-NULL PCCERT_CONTEXT this returns MUST be released with
                     CertFreeCertificateContext exactly once by the caller, independent of the store
                     it came from - the code does this consistently, either immediately
                     (isProviderAlreadyTrusted) or via a `finally` around the `_signCatalog` call.

----------------------------------------------------------------------------------------------------
CertFreeCertificateContext
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certfreecertificatecontext

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : pCertContext (PCCERT_CONTEXT)
Return value : BOOL
Purpose      : Decrements the refcount of / releases a certificate context.

FFM notes    : `FunctionDescriptor.of(DW, PTR)`. See ownership note above; also called on the
               certificate this class itself creates (`CertCreateSelfSignCertificate`'s return
               value), in a `finally` in `_elevatedCreateAndTrustProvider`, after it has already
               been added to all three stores (adding a context to a store does not transfer or
               share ownership of the original context handle - the store keeps its own
               copy/reference).

----------------------------------------------------------------------------------------------------
CertStrToNameW
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certstrtonamew

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : dwCertEncodingType (DWORD), pszX500 (LPCWSTR), dwStrType (DWORD), pvReserved (must be
               NULL), pbEncoded (BYTE*, out, may be NULL to query size), pcbEncoded (DWORD*,
               in/out), ppszError (LPCWSTR*, optional out)
Return value : BOOL
Purpose      : Parses a human-readable X.500 distinguished-name string (e.g. "CN=Some Publisher")
               into its DER-encoded form, the format CertCreateSelfSignCertificate's subject
               parameter requires.

FFM notes    : `FunctionDescriptor.of(DW, DW, PTR, DW, PTR, PTR, PTR, PTR)` - 7 inputs. Called twice
               in `_elevatedCreateAndTrustProvider`, following the standard Win32 "size then fill"
               two-pass pattern: first with pbEncoded=NULL to learn the required buffer size via
               pcbEncoded, then again with a freshly allocated buffer of that size. dwStrType is
               always CERT_X500_NAME_STR(3); dwCertEncodingType is X509_ASN_ENCODING(1). ppszError
               is always MemorySegment.NULL - this code does not currently retrieve the detailed
               "which character was invalid" diagnostic CertStrToNameW can optionally provide, it
               only checks GetLastError.
FFM caveat   : because both calls read the SAME `subjectStr`/`cbSubject` MemorySegments, and the
               second call's output buffer is sized exactly from the first call's reported length,
               this pattern is correct but fragile - anyone changing the subject string encoding
               without re-running the sizing call would silently truncate.

----------------------------------------------------------------------------------------------------
CertCreateSelfSignCertificate
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certcreateselfsigncertificate

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : hCryptProvOrNCryptKey (HCRYPTPROV_OR_NCRYPT_KEY_HANDLE, optional), pSubjectIssuerBlob
               (PCERT_NAME_BLOB), dwFlags (DWORD), pKeyProvInfo (PCRYPT_KEY_PROV_INFO, optional),
               pSignatureAlgorithm (PCRYPT_ALGORITHM_IDENTIFIER, optional), pStartTime (PSYSTEMTIME,
               optional), pEndTime (PSYSTEMTIME, optional), pExtensions (PCERT_EXTENSIONS, optional)
Return value : PCCERT_CONTEXT for the newly built certificate, or NULL.
Purpose      : Builds an in-memory, self-signed X.509 certificate from a subject name, a private key
               handle, and an optional extension list - no certificate authority or network
               round-trip involved, which is exactly why this tool can produce a usable code-signing
               certificate entirely offline.

FFM notes    : `FunctionDescriptor.of(PTR, PTR, PTR, DW, PTR, PTR, PTR, PTR, PTR)` - 9 slots: 1
               return type (PTR/PCCERT_CONTEXT) followed by 8 argument layouts, one per parameter
               listed above. Called with:
               hCryptProvOrNCryptKey = the NCrypt key handle from Section C (hKey);
               pSubjectIssuerBlob = the CertStrToNameW-encoded "CN=<providerName>" blob; dwFlags = 0
               (neither CERT_CREATE_SELFSIGN_NO_KEY_INFO nor _NO_SIGN is set - the default behavior,
               i.e. the certificate IS signed and DOES carry a CERT_KEY_PROV_INFO_PROP_ID pointing
               back at the CNG key container); pKeyProvInfo = the hand-built CRYPT_KEY_PROV_INFO
               (Section B structures below); pSignatureAlgorithm = an explicit SHA256RSA OID (see
               caveat below); start/end time = NULL, NULL (defaults to "now" through "now + 1
               year"); pExtensions = the Enhanced Key Usage extension array built earlier in the
               same method.

Windows-specific behavior relevant to this implementation:
    If pSignatureAlgorithm were left NULL, Microsoft's own reference documents that the function
    defaults to SHA1RSA. This class deliberately never relies on that default - it always supplies
    an explicit SHA256RSA CRYPT_ALGORITHM_IDENTIFIER, because every other hashing step in this file
    (the catalog's own SIP digest, the SIGNER_SIGNATURE_INFO.algidHash used later) is likewise
    pinned to SHA-256, and an inconsistently-hashed certificate would be an easy and hard-to-notice
    source of a rejected signature. There is also a documented leap-day caveat around the
    auto-computed pEndTime (a certificate created without an explicit pEndTime on Feb 29 can trip on
    the "add one year" math); this code does not special-case that, since it always uses the default
    +1-year window and a certificate created on a leap day is an edge case rare enough it has not
    been addressed here - worth knowing about, not yet fixed.

----------------------------------------------------------------------------------------------------
CertAddCertificateContextToStore
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certaddcertificatecontexttostore

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : hCertStore (HCERTSTORE), pCertContext (PCCERT_CONTEXT), dwAddDisposition (DWORD),
               ppStoreContext (PCCERT_CONTEXT*, optional out - always NULL here)
Return value : BOOL
Purpose      : Copies a certificate context into a store (system stores in this case), optionally
               replacing an existing entry with the same identity.

FFM notes    : `FunctionDescriptor.of(DW, PTR, PTR, DW, PTR)`. dwAddDisposition is always
               CERT_STORE_ADD_REPLACE_EXISTING(3), so re-running the trust step is idempotent rather
               than erroring on "already present". Called three times per successful trust
               operation: once against CurrentUser\My (so the private-key-linked certificate itself
               is discoverable there later by CertFindCertificateInStore) and once each against
               LocalMachine\Root and LocalMachine\TrustedPublisher (which is the pair of stores that
               actually makes Windows treat a driver signed with this certificate as trusted, per
               standard Authenticode trust rules - Root establishes the certificate as a trust
               anchor, and TrustedPublisher marks it as an explicitly trusted publisher without
               which most driver-signature UI still warns).

----------------------------------------------------------------------------------------------------
CertGetCertificateContextProperty
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certgetcertificatecontextproperty

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : pCertContext (PCCERT_CONTEXT), dwPropId (DWORD), pvData (void*, out - NULL to query
               size only, as used here), pcbData (DWORD*, in/out size)
Return value : BOOL
Purpose      : Reads a named property off a certificate context. Used here purely as a presence
               probe: dwPropId = CERT_KEY_PROV_INFO_PROP_ID(2), pvData = NULL, so a nonzero return
               only confirms the property exists (and reports its size via pcbData) without actually
               reading it.

FFM notes    : `FunctionDescriptor.of(DW, PTR, DW, PTR, PTR)`. Called in `_destroyPrivateKey` to read
               back a certificate's CRYPT_KEY_PROV_INFO property (two-call size/fill pattern: first
               call with pvData=NULL to get the required size into pcbData, second call to actually
               fill the buffer) so the legacy CAPI1 key container it names can be deleted via
               CryptAcquireContextW(CRYPT_DELETEKEYSET) - see that entry below. A certificate with no
               such property (e.g. the public-only copies this backend installs into LocalMachine\Root
               and \TrustedPublisher) has no private key to destroy, so the first call returning 0 is
               treated as "nothing to do," not an error.

----------------------------------------------------------------------------------------------------
CertDeleteCertificateFromStore
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-certdeletecertificatefromstore

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : pCertContext (PCCERT_CONTEXT)
Return value : BOOL
Purpose      : Removes a certificate from the store it was found in. Per Microsoft's documentation,
               this function always frees the passed certificate context, regardless of whether the
               deletion itself succeeds or fails - the caller must not call CertFreeCertificateContext
               on it afterward either way.

FFM notes    : `FunctionDescriptor.of(DW, PTR)`. Called in a loop by `_deleteExistingCerts`
               (CurrentUser\My, LocalMachine\Root, LocalMachine\TrustedPublisher) to remove every
               pre-existing certificate matching the provider name before a new one is created, so
               repeated runs never accumulate duplicates under the same provider name. Each loop
               iteration re-queries CertFindCertificateInStore from scratch (pPrevCertContext=NULL)
               rather than continuing an enumeration, since deleting a context invalidates any
               enumeration position into the store.

----------------------------------------------------------------------------------------------------
CryptEncodeObjectEx
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-cryptencodeobjectex

DLL          : Crypt32.dll
Header       : wincrypt.h

Parameters   : dwCertEncodingType (DWORD), lpszStructType (LPCSTR - may also be a small integer cast
               to LPCSTR for "predefined" struct types), pvStructInfo (void*, the in-memory struct
               to encode), dwFlags (DWORD), pvEncodePara (optional, always NULL here), pvEncoded
               (out - either a caller buffer or, with CRYPT_ENCODE_ALLOC_FLAG, an out-pointer to a
               LocalAlloc'd buffer), pcbEncoded (DWORD*, in/out size)
Return value : BOOL
Purpose      : General-purpose "encode this in-memory struct into its DER/ASN.1 byte form" primitive
               - the single encoder this file reuses for two unrelated payloads (see below).

FFM notes    : `FunctionDescriptor.of(DW, DW, PTR, PTR, DW, PTR, PTR, PTR)` - 7 inputs. Always
               called with dwFlags = CRYPT_ENCODE_ALLOC_FLAG(0x8000), which tells Crypt32 to
               allocate the output buffer itself (via LocalAlloc under the hood) and hand back a
               pointer through pvEncoded - the caller must not supply its own buffer in this mode,
               and must free the result with LocalFree (see LocalFree above), which this code does
               in both call sites. Two distinct uses in this file: 1. lpszStructType =
               `_handleOf(X509_ENHANCED_KEY_USAGE)` (36, a predefined small-integer struct-type
               constant, same "cast a small int through the pointer slot" convention noted under
               CertOpenStore above) with a hand-built CERT_ENHKEY_USAGE struct as pvStructInfo, to
               encode the "this cert may be used for code signing" extension. 2. lpszStructType =
               the OID string "1.3.6.1.4.1.311.2.1.25" (SPC_CAB_DATA_OBJID, passed as an ANSI LPCSTR
               via `_astr`) with a hand-built SPC_LINK struct as pvStructInfo, to encode the catalog
               member's SIP-indirect "file link" payload (see Section D).

----------------------------------------------------------------------------------------------------
CryptSIPRetrieveSubjectGuid
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mssip/nf-mssip-cryptsipretrievesubjectguid

DLL          : Crypt32.dll (despite mssip.h being the SIP-family header)
Header       : mssip.h

Parameters   : FileName (LPCWSTR), hFileIn (HANDLE, optional), pgSubject (GUID*, out)
Return value : BOOL
Purpose      : Asks the installed Subject Interface Package (SIP) providers which GUID identifies
               the "kind of thing" a given file is, for Authenticode/WinVerifyTrust purposes - e.g.
               a PE file resolves to the PE SIP GUID, a cabinet file to the CAB SIP GUID, and so on.

FFM notes    : `FunctionDescriptor.of(DW, PTR, PTR, PTR)` - 3 inputs. Called once in
               `_elevatedCreateAndSignCatalog`, passing the already-open INF file handle as hFileIn.
               If it returns FALSE (no SIP provider claims to recognize a plain INF file, which is
               plausible since there is no standard "INF SIP" registered on most systems), the code
               falls back to using the well-known DRIVER_ACTION_VERIFY GUID directly (copied via
               `MemorySegment.copy` into the same 16-byte/4-byte-aligned buffer) rather than
               treating the failure as fatal - this fallback is a deliberate design choice in this
               file, not something Microsoft's documentation prescribes for this specific situation.

----------------------------------------------------------------------------------------------------
STRUCTURE: CRYPT_ATTR_BLOB / CERT_NAME_BLOB (generic CRYPTOAPI_BLOB shape)
----------------------------------------------------------------------------------------------------
Source       : general Win32 crypto-API knowledge - this "{cbData; pbData;}" shape (formally
               CRYPTOAPI_BLOB, aliased under several names such as CERT_NAME_BLOB and
               CRYPT_ATTR_BLOB depending on context) is one of the most fundamental, stable
               structures in wincrypt.h; documented at
               https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-crypt_attr_blob

Parameters   : Fields, byte offset, type:
                   cbData (0, DWORD) - number of bytes in the buffer pbData points to
                   pbData (8, BYTE*) - pointer to the raw byte buffer
Size         : 16 bytes, 8-byte aligned (the 4 bytes between cbData and pbData are x64 padding for
               pointer alignment, not a real field).
Purpose      : The universal "counted byte buffer" shape Crypt32 uses everywhere - encoded names,
               encoded extensions, digests, etc.

FFM notes    : Built by the private `_blob(arena, cbData, pbData)` helper. Used for the subject name
               blob passed into CertCreateSelfSignCertificate as pSubjectIssuerBlob.

----------------------------------------------------------------------------------------------------
STRUCTURE: CERT_ENHKEY_USAGE
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-cert_enhkey_usage

Fields       : cUsageIdentifier (0, DWORD - count), rgpszUsageIdentifier (8, LPSTR* - array of OID
               strings).
Size         : 16 bytes, 8-byte aligned.
Purpose      : Lists the Enhanced Key Usage OIDs a certificate extension should grant - here, a
               single-entry array containing only "Code Signing" (1.3.6.1.5.5.7.3.3).

FFM notes    : Built inline in `_elevatedCreateAndTrustProvider` as `ekuStruct`; `oidArray` is a
               1-element PTR array holding the ASCII-encoded OID string, passed as pvStructInfo to
               CryptEncodeObjectEx with lpszStructType = X509_ENHANCED_KEY_USAGE(36).

----------------------------------------------------------------------------------------------------
STRUCTURE: CERT_EXTENSION
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-cert_extension

Fields       : pszObjId (0, LPSTR), fCritical (8, BOOL), Value (16, CRYPT_ATTR_BLOB - the
               DER-encoded extension payload).
Size         : 32 bytes, 8-byte aligned (Value's own 16-byte blob shape occupies bytes 16-31).
Purpose      : One X.509 certificate extension entry - here, wraps the already DER-encoded Enhanced
               Key Usage payload under OID szOID_ENHANCED_KEY_USAGE = "2.5.29.37".

FFM notes    : fCritical is set to 0 (not marked critical) - a verifier that does not understand
               this extension is permitted to ignore it rather than reject the whole certificate,
               which is the conventional, safer choice for this kind of usage-scoping extension.

----------------------------------------------------------------------------------------------------
STRUCTURE: CERT_EXTENSIONS
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-cert_extensions

Fields       : cExtension (0, DWORD), rgExtension (8, PCERT_EXTENSION - array).
Size         : 16 bytes, 8-byte aligned.
Purpose      : The array wrapper CertCreateSelfSignCertificate's pExtensions parameter expects -
               here, always a single-element array containing the CERT_EXTENSION above.

----------------------------------------------------------------------------------------------------
STRUCTURE: CRYPT_KEY_PROV_INFO
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-crypt_key_prov_info

Fields       : pwszContainerName (0, LPWSTR), pwszProvName (8, LPWSTR), dwProvType (16, DWORD),
               dwFlags (20, DWORD), cProvParam (24, DWORD), rgProvParam (32, PCRYPT_KEY_PROV_PARAM -
               array, unused here -> NULL), dwKeySpec (40, DWORD).
Size         : 48 bytes, 8-byte aligned.
Purpose      : Tells CertCreateSelfSignCertificate (and, later, anything that reads the resulting
               certificate's CERT_KEY_PROV_INFO_PROP_ID) which provider and key container the
               certificate's private key actually lives in, so it can be re-acquired without
               re-deriving it.

FFM notes    : pwszProvName is NULL (selects the default RSA Full CSP rather than naming one
               explicitly), dwProvType is PROV_RSA_FULL(1), dwFlags is CRYPT_MACHINE_KEYSET(0x20), and
               dwKeySpec is AT_SIGNATURE(2) - the standard combination for a private key created via the
               legacy CryptoAPI (CAPI1) CryptAcquireContextW/CryptGenKey rather than CNG. This backend
               previously provisioned the key via CNG (NCryptOpenStorageProvider/NCryptCreatePersistedKey)
               with dwProvType=0/dwKeySpec=CERT_NCRYPT_KEY_SPEC, which is the documented-correct
               combination for a CNG key but turned out to be incompatible with how SignerSignEx acquires
               private keys - see the "Investigation history" note below for why this backend switched to
               CAPI1 key provisioning entirely.

----------------------------------------------------------------------------------------------------
STRUCTURE: CRYPT_ALGORITHM_IDENTIFIER
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-crypt_algorithm_identifier

Fields       : pszObjId (0, LPSTR - algorithm OID), Parameters (8, CRYPT_OBJID_BLOB - optional
               algorithm parameters blob).
Size         : 24 bytes, 8-byte aligned.
Purpose      : Identifies a signature/hash algorithm by OID, with room for algorithm-specific
               parameters.

FFM notes    : Used once, as `sigAlgId`, to force CertCreateSelfSignCertificate's signature
               algorithm to SHA256RSA (OID "1.2.840.113549.1.1.11") instead of letting it default to
               SHA1RSA (see the CertCreateSelfSignCertificate caveat above). Parameters is left
               empty (cbData=0, pbData=NULL), which is normal for RSA-with-hash OIDs that do not
               need extra encoded parameters.

----------------------------------------------------------------------------------------------------
STRUCTURE: CERT_CONTEXT (partial - reinterpret only)
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-cert_context

Fields       : Only the ones relevant to this file:
                   dwCertEncodingType (0, DWORD), pbCertEncoded (8, BYTE*),
                   cbCertEncoded (16, DWORD), pCertInfo (24, PCERT_INFO),
                   hCertStore (32, HCERTSTORE).
Size         : 40 bytes, 8-byte aligned.
Purpose      : The structure behind every PCCERT_CONTEXT handle Crypt32 hands out; this file only
               cares about pulling the raw DER bytes back out of an already-created certificate.

FFM notes    : The code never allocates this struct - it only calls `certCtx.reinterpret(40)` on the
               MemorySegment CertCreateSelfSignCertificate returned, purely to read
               pbCertEncoded/cbCertEncoded back out (to obtain the exported DER bytes as a Java
               byte[] via `pbCertEncoded.reinterpret(cbCertEncoded).toArray(...)`). This relies on
               Microsoft's ABI guarantee that PCCERT_CONTEXT really does point at a
               CERT_CONTEXT-shaped block with these fields at these offsets - true and stable across
               all supported Windows versions, but worth flagging because `reinterpret` bypasses the
               FFM API's normal bounds checking, so getting this offset table wrong would silently
               read garbage or crash rather than throw a clear Java exception.


====================================================================================================
SECTION C - Private Key Provisioning Layer (Advapi32.dll)
====================================================================================================

This section creates the actual RSA private key the self-signed certificate is built around, using
the legacy CryptoAPI (CAPI1) provider/key model (CryptAcquireContextW/CryptGenKey) rather than the
modern Cryptography API: Next Generation (CNG) key-storage API. CNG was tried first, since Microsoft
recommends it for new code, but had to be abandoned - see the "Investigation history" note in
SECTION E below: SignerSignEx's internal private-key acquisition only follows the legacy
CryptAcquireContext path unless given an NCrypt-allowing flag, which it is not, so a CNG-provisioned
key was unusable by this backend's (now superseded) direct-SignerSignEx signing step regardless of
how correctly CRYPT_KEY_PROV_INFO otherwise described it.

----------------------------------------------------------------------------------------------------
CryptAcquireContextW
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-cryptacquirecontextw

DLL          : Advapi32.dll
Header       : wincrypt.h

Parameters   : phProv (HCRYPTPROV*, out), pszContainer (LPCWSTR, key container name), pszProvider
               (LPCWSTR, optional - NULL selects the default RSA Full CSP), dwProvType (DWORD),
               dwFlags (DWORD)
Return value : BOOL
Purpose      : Acquires a handle to a legacy CryptoAPI (CAPI1) key container, creating it first if
               CRYPT_NEWKEYSET is set, or permanently deleting it if CRYPT_DELETEKEYSET is set. This
               backend uses it both to provision the self-signed certificate's private key
               (CRYPT_NEWKEYSET) and, later, to destroy that same key container once it is no longer
               needed (CRYPT_DELETEKEYSET).

FFM notes    : `FunctionDescriptor.of(DW, PTR, PTR, PTR, DW, DW)` - 5 inputs. Provisioning call (in
               `_elevatedCreateAndTrustProvider`): pszContainer = a randomly-suffixed container name
               (`providerName + "_" + <64-bit SecureRandom hex>` - deliberate, so re-running the trust
               step never collides with a previous run's leftover key container name) and
               dwFlags = CRYPT_NEWKEYSET | CRYPT_MACHINE_KEYSET | CRYPT_SILENT - CRYPT_MACHINE_KEYSET
               so the key is provisioned in the machine key store (matching where the certificate
               itself is installed, in LocalMachine\Root and \TrustedPublisher), and CRYPT_SILENT to
               suppress any UI the CSP might otherwise try to show. Since the container name is always
               new, CRYPT_NEWKEYSET is always correct - there is never an existing container to reopen.
               Deletion call (in `_destroyPrivateKey`): pszContainer = the container name read back out
               of the certificate's CRYPT_KEY_PROV_INFO property (see
               CertGetCertificateContextProperty above), and dwFlags = CRYPT_DELETEKEYSET, OR'd with
               CRYPT_MACHINE_KEYSET only if that same flag was present in the stored provider info's
               dwFlags (a CRYPT_DELETEKEYSET call must target the same key-store scope the container
               actually lives in, or CryptAcquireContextW cannot find it to delete it). Per Microsoft's
               documentation, a successful CRYPT_DELETEKEYSET call permanently destroys the key
               material - it cannot be recovered.

----------------------------------------------------------------------------------------------------
CryptGenKey
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-cryptgenkey

DLL          : Advapi32.dll
Header       : wincrypt.h

Parameters   : hProv (HCRYPTPROV), Algid (ALG_ID, DWORD), dwFlags (DWORD - low word reserved for
               flags like CRYPT_EXPORTABLE, high word encodes the key length in bits), phKey
               (HCRYPTKEY*, out)
Return value : BOOL
Purpose      : Generates a key pair in the given CAPI1 provider context.

FFM notes    : `FunctionDescriptor.of(DW, PTR, DW, DW, PTR)` - 4 inputs. Called with
               Algid = AT_SIGNATURE(2) and dwFlags = (2048 << 16) | CRYPT_EXPORTABLE(0x1) to generate
               a 2048-bit, exportable RSA signing key.

----------------------------------------------------------------------------------------------------
CryptDestroyKey / CryptReleaseContext
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-cryptdestroykey
               https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/nf-wincrypt-cryptreleasecontext

DLL          : Advapi32.dll
Header       : wincrypt.h

Parameters   : CryptDestroyKey - hKey (HCRYPTKEY). CryptReleaseContext - hProv (HCRYPTPROV), dwFlags
               (DWORD, must be 0).
Return value : BOOL
Purpose      : Release the key handle and provider context handle CryptGenKey/CryptAcquireContextW
               returned, mirroring the CertFreeCertificateContext/LocalFree cleanup already done for
               the certificate and EKU encoding in the same `finally` block.

FFM notes    : `FunctionDescriptor.of(DW, PTR)` and `FunctionDescriptor.of(DW, PTR, DW)` respectively.


====================================================================================================
SECTION D - Catalog Building Layer (Wintrust.dll)
====================================================================================================

This section builds a v2 security catalog (.cat file) for the INF, the mechanism Windows driver
installation uses to verify the INF's integrity without requiring the INF itself to be
embedded-signed (INF files cannot carry an Authenticode signature the way a PE file can - a catalog
is the standard indirection Microsoft designed for exactly this case).

----------------------------------------------------------------------------------------------------
CryptCATAdminAcquireContext2
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatadminacquirecontext2

DLL          : Wintrust.dll
Header       : mscat.h

Parameters   : phCatAdmin (HCATADMIN*, out), pgSubsystem (const GUID*), pwszHashAlgorithm (LPCWSTR,
               optional), pStrongHashPolicy (PCCERT_STRONG_SIGN_PARA, optional), dwFlags (DWORD)
Return value : BOOL
Purpose      : Acquires a catalog-administration context scoped to a given "subsystem" GUID (which
               SIP/verification family the catalog is for) and hash algorithm - required before any
               other CryptCATAdmin*/ CryptCAT* call in this family can be used.

FFM notes    : `FunctionDescriptor.of(DW, PTR, PTR, PTR, PTR, DW)`. pgSubsystem is always the
               DRIVER_ACTION_VERIFY GUID (see the GUID structure entry below) - this scopes the
               whole catalog to the driver installation verification subsystem, matching what
               pnputil/the Device Installer actually validates against later. pwszHashAlgorithm is
               always "SHA256" (matching the SHA-256 hash used consistently throughout the file, and
               matching what WindowsDriverInstaller_PS1's New-FileCatalog defaults to as well, so
               both backends produce catalogs hashed the same way). pStrongHashPolicy and dwFlags
               are NULL/0 (no extra policy enforcement requested).

----------------------------------------------------------------------------------------------------
CryptCATAdminCalcHashFromFileHandle2
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatadmincalchashfromfilehandle2

DLL          : Wintrust.dll
Header       : mscat.h

Parameters   : hCatAdmin (HCATADMIN), hFile (HANDLE), pcbHash (DWORD*, in/out), pbHash (BYTE*, out,
               optional to query size), dwFlags (DWORD)
Return value : BOOL
Purpose      : Computes the file digest (using the hash algorithm bound to hCatAdmin, here SHA-256)
               that will be embedded as this catalog member's identifying hash.

FFM notes    : `FunctionDescriptor.of(DW, PTR, PTR, PTR, PTR, DW)`. Same two-pass size-then-fill
               pattern as CertStrToNameW: first call with pbHash=NULL to learn the digest length via
               pcbHash, then again with an allocated buffer of that length. The resulting bytes are
               both kept as a raw MemorySegment (`hashBuf`, later embedded directly into the
               SIP_INDIRECT_DATA struct as the Digest blob) and separately hex-encoded into `hexTag`
               (used as the member's reference tag string passed to CryptCATPutMemberInfo).

----------------------------------------------------------------------------------------------------
CryptCATAdminReleaseContext
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatadminreleasecontext

DLL          : Wintrust.dll
Header       : mscat.h

Parameters   : hCatAdmin (HCATADMIN), dwFlags (DWORD)
Return value : BOOL
Purpose      : Releases a context obtained from CryptCATAdminAcquireContext2.

FFM notes    : `FunctionDescriptor.of(DW, PTR, DW)`. Called with flags=0 in the outer `finally` of
               `_elevatedCreateAndSignCatalog`, after the INF file handle has already been closed.

----------------------------------------------------------------------------------------------------
CryptCATOpen
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatopen

DLL          : Wintrust.dll
Header       : mscat.h

Parameters   : pwszFileName (LPWSTR), fdwOpenFlags (DWORD), hProv (HCRYPTPROV, optional -> 0 here),
               dwPublicVersion (DWORD), dwEncodingType (DWORD)
Return value : HANDLE to the open catalog, or INVALID_HANDLE_VALUE.
Purpose      : Creates or opens a .cat catalog file for editing.

FFM notes    : `FunctionDescriptor.of(PTR, PTR, DW, PTR, DW, DW)`. Called with fdwOpenFlags =
               CRYPTCAT_OPEN_CREATENEW(0x1) (always creates a fresh catalog rather than opening an
               existing one - consistent with this code always deriving `catPath` fresh from the INF
               path) and dwPublicVersion = CRYPTCAT_VERSION_2(0x200), i.e. this file only ever
               produces version-2 catalogs (the modern format; version 1 is legacy and not relevant
               to a Windows-10+-only backend). dwEncodingType is passed as 0 in this call, differing
               slightly from CRYPT_ASN_ENCODING used elsewhere in the file - this matches the
               conventional usage for CryptCATOpen (the encoding-type parameter here scopes
               attribute encoding within the catalog itself, and 0/unspecified is an accepted value
               for this call specifically). The returned handle is checked against both NULL and
               INVALID_HANDLE_VALUE(-1), since Microsoft's own documentation is inconsistent across
               the mscat.h function family about which of the two failure sentinels a given catalog
               function uses.

----------------------------------------------------------------------------------------------------
CryptCATPersistStore
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatpersiststore

DLL          : Wintrust.dll
Header       : mscat.h

Parameters   : hCatalog (HCATINFO)
Return value : BOOL
Purpose      : Writes the in-memory catalog object's accumulated members/attributes out to the
               physical .cat file on disk. This is the call that actually flushes content -
               CryptCATClose alone only releases the handle.

FFM notes    : `FunctionDescriptor.of(DW, PTR)`. Called in the `finally` around the
               member/attribute-building block, immediately before CryptCATClose. Its omission was
               a real, confirmed bug found via CI (run 90195131262): every CryptCATOpen/
               CryptCATPutMemberInfo/CryptCATPutAttrInfo/CryptCATClose call reported success, yet the
               resulting .cat file was 0 bytes on disk, which signtool.exe then rejected as an
               unrecognized file format - CryptCATClose was closing a catalog that had never actually
               been persisted. Both this call's and CryptCATClose's return values are checked (the
               handle is still always closed regardless of whether persisting succeeded).

----------------------------------------------------------------------------------------------------
CryptCATClose
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatclose

DLL          : Wintrust.dll
Header       : mscat.h

Parameters   : hCatalog (HANDLE)
Return value : BOOL
Purpose      : Releases a catalog handle opened by CryptCATOpen. Does NOT itself flush accumulated
               members/attributes to disk - see CryptCATPersistStore above, which must be called
               first for the .cat file to actually contain anything.

FFM notes    : `FunctionDescriptor.of(DW, PTR)`. Called in the `finally` around the
               member/attribute-building block, right after CryptCATPersistStore - this ordering
               matters: the handle must still be released even on a partial-failure path (in this
               code's case, since every step before this can also return early on failure, an early
               return generally means the catalog was closed with an incomplete member and would
               need to be regenerated on retry rather than reused).

----------------------------------------------------------------------------------------------------
CryptCATPutMemberInfo
----------------------------------------------------------------------------------------------------
Source             : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatputmemberinfo

DLL                : Wintrust.dll
Header             : mscat.h

Parameters         : hCatalog (HANDLE), pwszFileName (LPWSTR, optional), pwszReferenceTag (LPWSTR),
                     pgSubjectType (GUID*), dwCertVersion (DWORD), cbSIPIndirectData (DWORD),
                     pbSIPIndirectData (BYTE*)
Return value       : CRYPTCATMEMBER* (owned by the catalog, caller must not free), or NULL on
                     failure.
Purpose            : Adds one member entry to an open catalog, keyed by a GUID identifying what
                     "kind" of subject it is (matching whatever SIP subsystem will later verify it)
                     and carrying that subject's SIP-indirect digest data.

FFM notes          : `FunctionDescriptor.of(PTR, PTR, PTR, PTR, PTR, DW, DW, PTR)` - 7 inputs.
                     pwszFileName is the INF's bare filename; pwszReferenceTag is the hex-encoded
                     SHA-256 digest string (`hexTag`); pgSubjectType is whatever GUID
                     CryptSIPRetrieveSubjectGuid resolved (or the DRIVER_ACTION_VERIFY fallback);
                     dwCertVersion is 0; cbSIPIndirectData/pbSIPIndirectData point at the 64-byte
                     SIP_INDIRECT_DATA struct built just before this call (see below). Per
                     Microsoft's own reference, these last two parameters are not optional despite
                     being usable with a NULL/0 pair in older, simpler catalog tools that skip the
                     extra indirect-data step - this file always supplies them.
Ownership/lifetime : the returned CRYPTCATMEMBER* belongs to the catalog object itself; it is used
                     immediately as an input to the two CryptCATPutAttrInfo calls that follow and is
                     never freed independently - it becomes invalid once CryptCATClose runs.

----------------------------------------------------------------------------------------------------
CryptCATPutAttrInfo
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/mscat/nf-mscat-cryptcatputattrinfo

DLL          : Wintrust.dll
Header       : mscat.h

Parameters   : hCatalog (HANDLE), pCatMember (CRYPTCATMEMBER*), pwszReferenceTag (LPWSTR),
               dwAttrTypeAndAction (DWORD), cbData (DWORD), pbData (BYTE*)
Return value : CRYPTCATATTRIBUTE* (owned by the catalog member, caller must not free), or NULL on
               failure.
Purpose      : Attaches one named, typed attribute to an existing catalog member.

FFM notes    : `FunctionDescriptor.of(PTR, PTR, PTR, PTR, DW, DW, PTR)` - 6 inputs. Called once per
               member, with dwAttrTypeAndAction = CRYPTCAT_ATTR_AUTHENTICATED(0x10000000) |
               CRYPTCAT_ATTR_NAMEASCII (0x1) | CRYPTCAT_ATTR_DATAASCII(0x10000) - i.e. an
               authenticated (covered by the catalog's own signature) attribute whose name is ASCII
               and whose data should be taken as-is rather than base64-decoded. The one attribute
               added is "File" (the lowercased INF filename, UTF-16LE-encoded, matching what
               catalog-consuming tooling conventionally expects for this attribute). cbData is
               computed as `(string.length() + 1) * 2` - i.e. character count including the NUL
               terminator, times 2 bytes per UTF-16 code unit - matching the UTF-16LE encoding
               `_wstr` produces for pbData.

               An earlier version also added a second, "OSAttr" attribute (literal string "10.0",
               intended to scope the member to Windows 10/11) - removed, see SECTION H "ITEM 4":
               it caused SetupCopyOEMInfW to reject the member outright
               (SPAPI_E_FILE_HASH_NOT_IN_CATALOG) rather than merely narrowing its applicability.
               WindowsDriverInstaller_PS1's New-FileCatalog -CatalogVersion 2.0 (proven working
               end-to-end on live CI) never sets this attribute either.

----------------------------------------------------------------------------------------------------
STRUCTURE: GUID
----------------------------------------------------------------------------------------------------
Source       : general, extremely stable Win32 knowledge (guiddef.h); shape documented at
               https://learn.microsoft.com/en-us/windows/win32/api/guiddef/ns-guiddef-guid

Fields       : Data1 (0, DWORD), Data2 (4, WORD), Data3 (6, WORD), Data4 (8, BYTE[8]).
Size         : 16 bytes, 4-byte aligned (note: 4-byte alignment, not 8 - GUID is one of the few
               structures in this file that is NOT 8-byte aligned, because it contains no
               pointer-sized fields).
Purpose      : The standard 128-bit Windows GUID/UUID representation.

FFM notes    : Built by the private `_guid(arena, long[] parts)` helper, which takes an 11-element
               long[] (Data1, Data2, Data3, then the 8 bytes of Data4 each as a separate element) -
               this input shape mirrors how a GUID literal is normally written out in C
               (`{0xF750E6C3,0x38EE,0x11d1,{0x85,0xE5,0x00,0xC0,0x4F,0xC2,0x95,0xEE}}`). Used for
               DRIVER_ACTION_VERIFY (the well-known GUID F750E6C3-38EE-11D1-85E5-00C04FC295EE,
               defined in softpub.h, that identifies the driver-verification SIP action/subsystem -
               this value was cross-checked byte-for-byte against the public softpub.h definition
               and is correct in this file) and for the per-file subject GUID buffer that
               CryptSIPRetrieveSubjectGuid fills in (or that DRIVER_ACTION_VERIFY_PARTS is copied
               into as a fallback).

----------------------------------------------------------------------------------------------------
STRUCTURE: SPC_LINK - VERIFIED
----------------------------------------------------------------------------------------------------
Source    : No Microsoft Learn prose page exists for this structure (a direct check at the expected
            learn.microsoft.com wintrust/ns-wintrust-spc_link URL returns 404). Field composition is
            instead confirmed via two other official Microsoft sources:
                (1) WinTrust.h, the Windows SDK/WDK header, and
                (2) Microsoft's win32metadata-generated API reference, at
                    https://microsoft.github.io/windows-docs-rs/doc/windows/Win32/Security/WinTrust/struct.SPC_LINK.html
            Both confirm the same dwLinkChoice (DWORD) + union {pwszUrl, Moniker, pwszFile}
            composition, with named constants SPC_URL_LINK_CHOICE=1 / SPC_MONIKER_LINK_CHOICE=2 /
            SPC_FILE_LINK_CHOICE=3, matching this file's own dwLinkChoice=3 (pwszFile) exactly.
            Neither source states byte offsets; the offsets below (0 and 8) are derived from
            standard x64 struct-layout/alignment rules, the same derivation used throughout this
            document.

Fields    : As built in this file:
                dwLinkChoice (0, DWORD), then a union at offset 8 whose active member here is
                pwszFile (LPWSTR) - i.e. this code only ever exercises the "this SPC_LINK identifies
                a file, by name" variant (dwLinkChoice = 3 = SPC_FILE_LINK_CHOICE, in the code's own
                naming), never the URL or "moniker" (serialized-object) variants the same union can
                hold in a fuller SPC_LINK implementation.
Size      : 16 bytes, 8-byte aligned, matching what's allocated in this file.
Purpose   : As used here:
                a placeholder reference to the "subject file" this piece of SIP-indirect data
                describes, using the conventional literal string "<<<Obsolete>>>" as the file name -
                a long-standing Authenticode convention (predating this file, not invented here) for
                marking this link as intentionally non-resolvable/not meant to be followed, used
                when the catalog itself (not this embedded link) is the actual source of truth for
                locating the subject.

FFM notes : Encoded via CryptEncodeObjectEx with lpszStructType = "1.3.6.1.4.1.311.2.1.25"
            (SPC_CAB_DATA_OBJID) before being embedded into the SIP_INDIRECT_DATA struct below -
            this specific OID choice for a non-CAB, non-PE (INF) subject remains a separate,
            still-open question - unrelated to the struct layout verified above - see the
            file-level comment block near the top of WindowsDriverInstaller_FFM.java for the open
            question this raises (whether pnputil validates this successfully on a real machine, and
            what to check first if not).

----------------------------------------------------------------------------------------------------
STRUCTURE: SIP_INDIRECT_DATA - VERIFIED
----------------------------------------------------------------------------------------------------
Source    : https://learn.microsoft.com/en-us/windows/win32/api/mssip/ns-mssip-sip_indirect_data

Fields    : As built in this file (three 24/24/16-byte groups back to back, matching the
            Learn-documented {Data, DigestAlgorithm, Digest} fields) :
                Data.pszObjId            ( 0, LPSTR) - here, the same SPC_CAB_DATA_OBJID
                Data.cbData              ( 8, DWORD) - length of the encoded SPC_LINK
                Data.pbData              (16, BYTE*) - the encoded SPC_LINK bytes
                DigestAlgorithm.pszObjId (24, LPSTR) - here, szOID_NIST_sha256
                                                       ("2.16.840.1.101.3.4.2.1")
                DigestAlgorithm.cbData   (32, DWORD) - 0 (no algorithm parameters)
                DigestAlgorithm.pbData   (40, BYTE*) - NULL
                Digest.cbData            (48, DWORD) - the SHA-256 digest length
                Digest.pbData            (56, BYTE*) - the SHA-256 digest bytes
                                                       (`hashBuf`, the same buffer
                                                       CryptCATAdminCalcHashFromFileHandle2
                                                       filled in)
Size      : 64 bytes, 8-byte aligned.

Purpose   : As used here:
                the payload CryptCATPutMemberInfo's cbSIPIndirectData/pbSIPIndirectData parameters
                require - ties the catalog member to both "what file this claims to be about"
                (indirectly, via the encoded SPC_LINK) and "what its digest is" (directly, via the
                embedded SHA-256 bytes), which is what a verifier reads back to confirm the catalog
                member matches the actual on-disk INF.


====================================================================================================
====================================================================================================
SECTION E - Authenticode Signing Layer (Mssign32.dll) - ACTIVE
====================================================================================================

Actual catalog signing is performed directly by the SignerSignEx call documented in this section (see
_signCatalog in WindowsDriverInstaller_FFM.java). It is built from Microsoft Learn "conceptual"
reference pages rather than a header file - Microsoft explicitly documents these functions and
structures on learn.microsoft.com, but (per Microsoft's own notes on those pages) does not ship a
header declaring them; a caller is expected to declare the prototypes itself and dynamically resolve
the DLL export, which is exactly the pattern this file follows via SymbolLookup instead of
LoadLibrary/GetProcAddress.

----------------------------------------------------------------------------------------------------
SignerSignEx
----------------------------------------------------------------------------------------------------
Source             : https://learn.microsoft.com/en-us/windows/win32/seccrypto/signersignex

DLL                : Mssign32.dll
Header             : none (explicitly undeclared by Microsoft; see above)
Calling convention : WINAPI (__stdcall) Parameters (9, in the exact order below - VERIFIED against
                     Microsoft Learn): dwFlags (DWORD, in), pSubjectInfo (SIGNER_SUBJECT_INFO*, in),
                     pSignerCert (SIGNER_CERT*, in), pSignatureInfo (SIGNER_SIGNATURE_INFO*, in),
                     pProviderInfo (SIGNER_PROVIDER_INFO*, in, optional), pwszHttpTimeStamp
                     (LPCWSTR, in, optional), psRequest (PCRYPT_ATTRIBUTES, in, optional), pSipData
                     (LPVOID, in, optional), ppSignerContext (SIGNER_CONTEXT**, out)

Return value       : HRESULT (S_OK on success)
Purpose            : Signs the specified file and returns a pointer to the signed data - the original,
                     simplest member of this function family (minimum client Windows XP). Unlike
                     SignerSignEx2/SignerSignEx3, it has no dwTimestampFlags/pszTimestampAlgorithmOid
                     pair (only a single pwszHttpTimeStamp URL, implying Authenticode-style
                     timestamping only), no pCryptoPolicy, and no pDigestSignInfo. This file switched
                     from SignerSignEx3 to SignerSignEx after SignerSignEx3 reproduced the identical
                     E_INVALIDARG (see the "Investigation history" note below) - trying the smallest
                     member of the family narrows whether the failure depends on anything in the
                     newer parameters' machinery, even when they are all passed as NULL/0.

FFM notes          : `FunctionDescriptor.of(DW, DW, PTR, PTR, PTR, PTR, PTR, PTR, PTR, PTR)` - 10
                     slots: 1 return type (DW/HRESULT) followed by 9 argument layouts, one per
                     parameter listed above. Called in `_signCatalog` with: dwFlags=0 (no
                     PE-page-hash flags - irrelevant here since the subject is a .cat file, not a
                     PE); pProviderInfo=NULL (meaning "the certificate itself must already be
                     associated with a usable CSP/KSP", satisfied here since pSignerCert routes
                     through a certificate store lookup rather than a bare SPC file);
                     pwszHttpTimeStamp=NULL, psRequest=NULL (no timestamping requested - the produced
                     signature has no trusted-timestamp counter-signature, meaning it will rely
                     solely on the signing certificate's own (1-year, per Section B) validity window
                     rather than surviving past that window the way a timestamped signature would);
                     pSipData=NULL. The out-parameter ppSignerContext is always read back and, if
                     non-NULL, freed via SignerFreeSignerContext regardless of the overall HRESULT -
                     this matches Microsoft's own note (on the newer members of this family) that a
                     context may still have been allocated on certain partial-failure paths.

Windows-specific caveats relevant to this implementation:
    - Minimum supported client is Windows XP per Microsoft's own requirements table (older than both
      SignerSignEx2's Windows 8 and SignerSignEx3's Windows 10) - not a concern here since this
      backend already restricts itself to Windows 10+.
    - Microsoft's documented E_INVALIDARG case for SignerSignEx3 (dwTimestampFlags=
      SIGNER_TIMESTAMP_AUTHENTICODE combined with dwFlags=SIG_APPEND) does not apply here: this
      simpler entry point has no dwTimestampFlags parameter at all, and this file uses neither
      timestamping nor SIG_APPEND.

Investigation history - SignerSignEx/SignerSignEx2/SignerSignEx3 E_INVALIDARG (HRESULT=0x80070057),
RESOLVED BY PIVOTING AWAY FROM THIS CALL:
    Live Windows CI (GitHub Actions, mutating/UAC-enabled run) consistently reported E_INVALIDARG from
    this call, unchanged across every variation tried:
      - Every field of every struct passed to the call (SIGNER_SUBJECT_INFO, SIGNER_FILE_INFO,
        SIGNER_CERT, SIGNER_CERT_STORE_INFO, SIGNER_SIGNATURE_INFO, SIGNER_ATTR_AUTHCODE,
        CRYPT_ATTRIBUTES/CRYPT_ATTRIBUTE) was checked against Microsoft Learn and, for fields with
        more than one legal value (dwCertPolicy, dwAttrChoice, psAuthenticated, pdwIndex), tried in at
        least two configurations, including a genuine bug fix (SIGNER_SUBJECT_INFO.pdwIndex must be
        NULL itself, not a pointer to a zeroed DWORD).
      - All three entry points in the family were tried (SignerSignEx2 -> SignerSignEx3 ->
        SignerSignEx), with byte-identical struct dumps and the identical failure at each.
      - The certificate's private-key provisioning was rewritten from CNG to legacy CryptoAPI
        (CryptAcquireContextW+CryptGenKey, see those entries below) after a diagnostic
        CryptAcquireCertificatePrivateKey call showed the CNG-provisioned key was unusable
        (NTE_BAD_PROV_TYPE) - a real, independent bug, but confirmed orthogonal to the E_INVALIDARG
        once private-key acquisition itself started succeeding and the failure persisted unchanged.
      - The Windows CAPI2 operational event log was captured around a live CI run and showed zero
        internal work tied to the SignerSignEx call itself - it rejects its arguments at the very
        first line, before anything CAPI2 instruments.
      - A signtool.exe cross-check (`.github/workflows/test-windows-driver-installer.yml`, "Cross-check
        catalog signing with signtool.exe" step) proved decisive: signtool.exe successfully signs a
        real `.cat` file (built with makecat.exe) using the exact same self-signed certificate this
        backend creates and trusts. This conclusively ruled out the certificate, private key, and CI
        environment - the defect is specific to this backend's own SignerSignEx call.
      - A COM-initialization hypothesis (CoInitializeEx/CoUninitialize around the call, since
        signtool.exe is a native EXE while this backend calls from an uninitialized JVM thread) was
        tried last and also did not change the failure.
    With every struct-content permutation, all three entry points, the key-provisioning mechanism, and
    COM initialization exhausted without the failure ever changing shape, this call was abandoned at
    the time in favor of shelling out to signtool.exe as an external process instead - see
    "Investigation history, part 2" below for how the real bugs were later found and this call
    restored as the active signing path.

Investigation history, part 2 - REVERSED: two struct fields were wrong at the same time and masked each
other across the entire "every struct-content permutation" sweep above:
    A later, more careful re-check of every field's actual semantics (not just the values already
    tried) against SIGNER_SUBJECT_INFO and SIGNER_CERT_STORE_INFO's documented behavior found two
    separate errors that were both present throughout part 1's investigation, so no single-field
    permutation in that sweep ever isolated either one on its own:
      - SIGNER_CERT_STORE_INFO.hCertStore was set to the CurrentUser\My store handle (hMy) - VERIFIED
        against https://learn.microsoft.com/en-us/windows/win32/seccrypto/signer-cert-store-info :
        hCertStore is only consulted when dwCertPolicy is SIGNER_CERT_POLICY_STORE (search that store
        for a certificate matching pSigningCert) or SIGNER_CERT_POLICY_CHAIN_NO_ROOT; with
        dwCertPolicy=SIGNER_CERT_POLICY_CHAIN (build the certificate chain from pSigningCert directly),
        which this backend has always used, hCertStore must be NULL. Part 1's reasoning ("two
        independently-observed working callers... pass CHAIN together with a real store handle") did
        not hold up on closer inspection of that same documentation page.
      - SIGNER_SUBJECT_INFO.pdwIndex - RECONSTRUCTED / INFERRED (no Microsoft Learn page shows a worked
        example): part 1 concluded this must be NULL itself ("a genuine bug fix", see above). Re-review
        found that conclusion backwards - "reserved, must be set to zero" describes the pointed-to
        DWORD's value, not the pointer field; pdwIndex must point at a real, writable DWORD holding 0,
        not be NULL. Both configurations were in fact tried during part 1, but always in combination
        with the hCertStore bug above still present, so neither ever ran against an otherwise-correct
        call.
      - SIGNER_SIGNATURE_INFO.psAuthenticated was left NULL throughout part 1 (see that struct's field
        comment, since corrected) - RECONSTRUCTED / INFERRED: populating it with the SPC_SP_OPUS_INFO/
        SPC_STATEMENT_TYPE attributes (built but never attached) was not itself confirmed to be
        required by any Microsoft documentation found, but is restored to match every known working
        caller of this pattern and is being re-tried alongside the two corrections above.
    CONFIRMED on live Windows CI (2026-08-31): createAndSignCatalog returned [0] end-to-end with all
    three corrections applied together, resolving the E_INVALIDARG from part 1.

----------------------------------------------------------------------------------------------------
SignerFreeSignerContext
----------------------------------------------------------------------------------------------------
Source       : general Mssign32/Signer* API family knowledge - this function is documented alongside
               the SIGNER_* structure pages, e.g. referenced from
               https://learn.microsoft.com/en-us/windows/win32/seccrypto/signersignex2
               ("free... by calling the SignerFreeSignerContext function").

DLL          : Mssign32.dll
Header       : none (undeclared, same as SignerSignEx)

Parameters   : pSignerContext (SIGNER_CONTEXT*)
Return value : HRESULT
Purpose      : Releases a SIGNER_CONTEXT allocated by a Signer* signing call.

FFM notes    : `FunctionDescriptor.of(DW, PTR)`. This file does not otherwise inspect the contents
               of the returned SIGNER_CONTEXT (e.g. to pull the signed blob back out) - it is only
               used as a success/failure signal via SignerSignEx's HRESULT, and the context itself
               is freed immediately once read.

----------------------------------------------------------------------------------------------------
STRUCTURE: SIGNER_SUBJECT_INFO - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/seccrypto/signer-subject-info

Fields       : cbSize (0, DWORD), pdwIndex (8, DWORD* - reserved, pointed-to DWORD must be zero),
               dwSubjectChoice (16, DWORD), union at 24 (active member here: pSignerFileInfo,
               SIGNER_FILE_INFO*).
Size         : 32 bytes, 8-byte aligned.
Purpose      : Identifies what is being signed - a file (this file's only used case, dwSubjectChoice
               = SIGNER_SUBJECT_FILE = 1) or a BLOB (SIGNER_SUBJECT_BLOB = 2, unused here).

FFM notes    : pdwIndex points at a real, writable DWORD holding 0 - Microsoft's own documentation for
               this member reads "This member is reserved. It must be set to zero," which describes
               the pointed-to DWORD's value, not the pointer field itself (an earlier round of this
               investigation misread it as requiring the pointer field to be NULL - see the
               "Investigation history, part 2" note below).

----------------------------------------------------------------------------------------------------
STRUCTURE: SIGNER_FILE_INFO - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/seccrypto/signer-file-info

Fields       : cbSize (0, DWORD), pwszFileName (8, LPCWSTR), hFile (16, HANDLE, optional - may be
               NULL).
Size         : 24 bytes, 8-byte aligned.
Purpose      : Identifies the specific file to sign, by path and/or by an already-open handle.

FFM notes    : pwszFileName is set to the .cat path; hFile is left MemorySegment.NULL - the code
               lets SignerSignEx open the file itself by path rather than supplying a pre-opened
               handle, which is the simpler and, per Microsoft's own description of this optional
               member, fully supported way to use this struct.

----------------------------------------------------------------------------------------------------
STRUCTURE: SIGNER_CERT - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/seccrypto/signer-cert

Fields       : cbSize (0, DWORD), dwCertChoice (4, DWORD), union at 8 (active member here:
               pCertStoreInfo, SIGNER_CERT_STORE_INFO*), hwnd (16, HWND - documented as currently
               unused/ignored by the function).
Size         : 24 bytes, 8-byte aligned.
Purpose      : Identifies the signing certificate - either an SPC file (SIGNER_CERT_SPC_FILE=1), a
               certificate-store lookup (SIGNER_CERT_STORE=2, what this file uses), or an SPC chain
               (SIGNER_CERT_SPC_CHAIN=3).

FFM notes    : dwCertChoice = SIGNER_CERT_STORE(2); hwnd is left NULL, matching Microsoft's note
               that this member is not currently used.

----------------------------------------------------------------------------------------------------
STRUCTURE: SIGNER_CERT_STORE_INFO - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/seccrypto/signer-cert-store-info

Fields       : cbSize (0, DWORD), pSigningCert (8, PCCERT_CONTEXT), dwCertPolicy (16, DWORD),
               hCertStore (24, HCERTSTORE, optional).
Size         : 32 bytes, 8-byte aligned.
Purpose      : Specifies both the exact signing certificate context and the policy for pulling in
               the rest of its chain from the given store when building the signature's certificate
               chain.

FFM notes    : pSigningCert = the certificate found earlier via CertFindCertificateInStore;
               dwCertPolicy = SIGNER_CERT_POLICY_CHAIN(0x2) (build the signature's certificate list
               from pSigningCert's own chain - this already checks the MY, CA, ROOT, and SPC stores
               per Microsoft's docs); hCertStore = NULL, since it is only consulted under
               SIGNER_CERT_POLICY_STORE/SIGNER_CERT_POLICY_CHAIN_NO_ROOT, not CHAIN (see the
               "Investigation history, part 2" note below).

----------------------------------------------------------------------------------------------------
STRUCTURE: SIGNER_SIGNATURE_INFO - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/seccrypto/signer-signature-info

Fields       : cbSize (0, DWORD), algidHash (4, ALG_ID), dwAttrChoice (8, DWORD), union at 16
               (active member is pAttrAuthcode, SIGNER_ATTR_AUTHCODE* - see below), psAuthenticated
               (24, PCRYPT_ATTRIBUTES, optional), psUnauthenticated (32, PCRYPT_ATTRIBUTES,
               optional).
Size         : 40 bytes, 8-byte aligned.
Purpose      : Specifies the hash algorithm for the signature and any extra Authenticode-specific or
               user-supplied signed/unsigned attributes.

FFM notes    : algidHash = CALG_SHA_256(0x0000800c) (the classic CryptoAPI ALG_ID constant for
               SHA-256 - consistent with every other hash choice in this file); dwAttrChoice =
               SIGNER_NO_ATTR(0) with the union pointer left NULL (no Authenticode-specific
               commercial/individual publisher attributes requested via that union). psAuthenticated
               now points to a CRYPT_ATTRIBUTES array carrying SPC_SP_OPUS_INFO and SPC_STATEMENT_TYPE
               attributes (see STRUCTURE: CRYPT_ATTRIBUTES below) - see the "Investigation history"
               note below for why. psUnauthenticated is left NULL. SIGNER_AUTHCODE_ATTR plus a
               populated SIGNER_ATTR_AUTHCODE (see below) was tried and reverted - see the
               "Investigation history" note below.

----------------------------------------------------------------------------------------------------
STRUCTURE: SIGNER_ATTR_AUTHCODE - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/seccrypto/signer-attr-authcode

Fields       : cbSize (0, DWORD), fCommercial (4, BOOL), fIndividual (8, BOOL), pwszName (16,
               LPCWSTR), pwszInfo (24, LPCWSTR).
Size         : 32 bytes, 8-byte aligned.
Purpose      : Specifies Authenticode-specific attributes (the display name/URL shown on download,
               and whether the subject is signed as a commercial or individual publisher) for a
               signature whose SIGNER_SIGNATURE_INFO.dwAttrChoice is SIGNER_AUTHCODE_ATTR.

FFM notes    : Not currently constructed - this backend now leaves SIGNER_SIGNATURE_INFO.dwAttrChoice
               at SIGNER_NO_ATTR, so this structure is unused. It was populated once, as part of the
               SignerSignEx2 E_INVALIDARG investigation (fCommercial = FALSE, fIndividual = FALSE,
               pwszName/pwszInfo both empty strings rather than NULL), then reverted without changing
               the failure - see the "Investigation history" note below.


----------------------------------------------------------------------------------------------------
STRUCTURE: CRYPT_ATTRIBUTES - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-crypt_attributes

Fields       : cAttr (0, DWORD), rgAttr (8, PCRYPT_ATTRIBUTE - array, not a linked list).
Size         : 16 bytes, 8-byte aligned.
Purpose      : A counted array of CRYPT_ATTRIBUTE entries; used here as
               SIGNER_SIGNATURE_INFO.psAuthenticated.

FFM notes    : cAttr = 2. rgAttr points to two contiguous CRYPT_ATTRIBUTE structures (SPC_SP_OPUS_INFO
               then SPC_STATEMENT_TYPE), built with MemorySegment.copy into one allocation rather than
               two separately-allocated structures, since rgAttr is a flat array.

----------------------------------------------------------------------------------------------------
STRUCTURE: CRYPT_ATTRIBUTE - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-crypt_attribute

Fields       : pszObjId (0, LPSTR), cValue (8, DWORD), rgValue (16, PCRYPT_ATTR_BLOB - array of
               CRYPT_ATTR_BLOB, one per value).
Size         : 24 bytes, 8-byte aligned.
Purpose      : One signed/unsigned attribute: an OID plus one or more DER-encoded values.

FFM notes    : cValue = 1 for both attributes this backend builds (SPC_SP_OPUS_INFO_OBJID and
               SPC_STATEMENT_TYPE_OBJID), each with a single CRYPT_ATTR_BLOB value built via the
               existing `_blob()` helper (see STRUCTURE: CRYPT_ATTR_BLOB below).

----------------------------------------------------------------------------------------------------
STRUCTURE: CRYPT_ATTR_BLOB - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/wincrypt/ns-wincrypt-crypt_integer_blob
               (same {DWORD cbData; BYTE *pbData;} shape reused across CRYPT_INTEGER_BLOB,
               CRYPT_ATTR_BLOB, CERT_NAME_BLOB, etc.)

Fields       : cbData (0, DWORD), pbData (8, BYTE*).
Size         : 16 bytes, 8-byte aligned.
Purpose      : A counted byte buffer - here, one DER-encoded ASN.1 value per CRYPT_ATTRIBUTE.rgValue
               entry.

FFM notes    : Built via the existing `_blob(Arena, int cbData, MemorySegment pbData)` helper (already
               used elsewhere in this file for CERT_NAME_BLOB-shaped fields). Wraps the hand-built
               SPC_SP_OPUS_INFO and SPC_STATEMENT_TYPE DER byte literals - see the "Investigation
               history" note below for the exact bytes and their source.


====================================================================================================
SECTION F - Miscellaneous Notes for Future Maintainers
====================================================================================================

FFM string-building helpers (`_wstr`, `_astr`):
    `_wstr` produces a NUL-terminated UTF-16LE buffer with 2-byte alignment - the correct native
    representation for every LPCWSTR/LPWSTR parameter in this file. `_astr` produces
    a NUL-terminated single-byte buffer with 1-byte alignment, used only for the handful of
    parameters that are genuinely ANSI/8-bit in the real Win32 prototype (OID strings passed as
    LPSTR - Crypt32's OID parameters are documented as narrow strings even in the "W" function
    variants, since OIDs are ASCII by definition and Windows does not define a wide-character OID
    type).

Arena discipline: every native call site in this file uses either the
    process-lifetime shared arena `_arena` (only for the resolved DLL/symbol lookups themselves, at
    class-init time - never for per-call data) or a `try (Arena arena = Arena.ofConfined()) { ... }`
    block scoped to a single logical operation. This means every MemorySegment built with `_wstr`/
    `_astr`/`_blob`/`_guid`/inline `arena.allocate(...)` calls is automatically reclaimed when its
    enclosing method returns - the only things that need EXPLICIT native-side cleanup are handles/
    contexts the Win32 APIs themselves allocated (kernel HANDLEs, HCERTSTORE, PCCERT_CONTEXT,
    NCRYPT_* handles, HCATADMIN, catalog HANDLEs, SIGNER_CONTEXT, and the two LocalFree-owned
    CryptEncodeObjectEx output buffers) - none of which the arena knows about or will free for you.
    This is the single most important invariant to preserve when editing this file: adding a new
    native call that returns a handle/context means adding a matching, correctly-ordered release
    call, almost always in a `finally` block, exactly like every existing call site does.

Build command: `make jar JDK_VER=8 && make jar JDK_VER=25` from `src/` is enough after editing any
    source file in this backend (or WindowsDriverInstaller_PS1.java). NOTE: the first `make` may do
    nothing. Java 8 toolchain: /usr/lib/jvm/java-1.8.0-openjdk-1.8.0.161-0.b14.el7_4.x86_64/bin. Java
    25 toolchain: /opt/openjdk-25_linux-x64_bin/jdk-25/bin.

Elevation architecture (ties Section A together with the rest of the file):
    `createAndTrustProvider`/`createAndSignCatalog` (the two public entry points that need
    elevation) never touch NCrypt/Crypt32/Wintrust/Mssign32 APIs directly from the calling
    (non-elevated) process - they only build a command line and call `_runElevatedSelf`, which
    relaunches this same JVM, elevated, with a hidden marker argument (`ELEVATED_OP_ARG`) that
    routes `main()` into `_elevatedCreateAndTrustProvider`/ `_elevatedCreateAndSignCatalog` instead
    of any normal entry point. The elevated child communicates its result back to the waiting parent
    exclusively through its process exit code (read via GetExitCodeProcess) and a temp log file it
    writes before exiting - there is no shared memory or pipe across the UAC elevation boundary,
    which is a genuine Windows constraint (an elevated child runs in a different security context
    and cannot easily inherit inherited standard-handle pipes from a non-elevated parent the way
    a same-privilege child process could).


====================================================================================================
SECTION G - CI Investigation Log (post-signtool.exe pivot)
====================================================================================================

CONFIRMED - CryptCATPersistStore fix works (CI run 90196100036):
    FFM.createAndSignCatalog now returns success. The .cat file was 432 bytes before signing
    (previously 0 - see the CryptCATPersistStore entry in Section D), and signtool.exe reported
    "Successfully signed" / "Number of errors: 0". The catalog-signing bug that started this whole
    investigation chain is resolved and verified end-to-end on a real Windows CI runner.

PARTIALLY FIXED, HYPOTHESIS REVISED (2026-08-30) - FFM.installDriver hangs indefinitely calling
pnputil.exe via ShellExecuteExW. Two CI runs so far:

CI run 90196100036 (first hang, with /install still present):
    Immediately after the catalog now signs successfully, installDriver
    (_shellExecuteElevatedAndWait, WindowsDriverInstaller_FFM.java line ~464) blocked forever in
    _WaitForSingleObject.invoke(hProcess, ...), waiting on the elevated
    "cmd.exe /c pnputil.exe /add-driver ... /install" child process. A Java thread dump captured
    automatically on cancellation showed the main thread still inside that native downcall after
    200+ seconds elapsed - well short of the method's own 5-minute internal timeout, confirming
    this was killed externally, not by the method's own TerminateProcess path.

    Leading hypothesis at the time: "pnputil.exe /add-driver <inf> /install" raises an interactive
    device-installation confirmation dialog that nothing can dismiss on a headless CI runner -
    SEI_nShow=SW_HIDE only hides the elevated cmd.exe console window, not a UI dialog raised by a
    child process. Fix applied (commit ac29c9f): both WindowsDriverInstaller_FFM.installDriver and
    WindowsDriverInstaller_PS1.installDriver now always call plain /add-driver, never /install.

CI run (second dispatch, /install already removed) - hypothesis only PARTIALLY confirmed:
    PS1.installDriver: FIXED. Returned immediately instead of hanging - though it then failed with
    "The third-party INF does not contain digital signature information", because PS1's driver was
    never actually signed (see the OPEN item 2 entry below) - pnputil rejected it outright before
    reaching any signature-trust evaluation, so this run doesn't actually prove /install was ever
    PS1's cause either; it just proves PS1 no longer hangs on an unsigned/rejected driver.

    FFM.installDriver: STILL HUNG. Thread dump again stuck in the same
    _shellExecuteElevatedAndWait call (~line 472), ~89s in, before what the log shows as a manual
    cancellation ("Error: The operation was canceled." - no timeout-minutes is set anywhere in the
    workflow, so this was the user canceling, not an automatic job timeout). /install was already
    absent this run, so it was NOT the (sole) cause of FFM's hang.

    Crucially, this same run's FFM.createAndTrustProvider and FFM.createAndSignCatalog - which use
    the exact same ShellExecuteExW+WaitForSingleObject mechanism via _runElevatedSelf - both
    completed successfully and quickly. So the native elevation plumbing itself is not broken;
    whatever hangs is specific to elevating "cmd.exe /c pnputil.exe /add-driver ...".

    REVISED hypothesis: PS1's driver had NO valid signature at all this run (blocked by item 2),
    so pnputil rejected it outright with a definitive error - no dialog possible. FFM's driver, by
    contrast, had a validly self-signed (non-WHQL / not Microsoft-verified) catalog - pnputil may
    raise a "Windows can't verify the publisher of this driver software" consent dialog
    specifically when it reaches a real-but-untrusted signature check, independent of /install.
    That would hang forever with no desktop session to dismiss it on. NOT YET PROVEN - PS1 has
    never gotten a validly-signed driver far enough to exercise the same code path.

    FIX ATTEMPTED (commit abbc84a), per explicit user decision: added the legacy
    HKLM\SOFTWARE\Microsoft\Driver Signing\Policy (REG_BINARY, first byte 0 = silently succeed)
    registry tweak to test-windows-driver-installer.yml's "Allow silent admin elevation" step,
    alongside the existing ConsentPromptBehaviorAdmin=0 tweak. CONFIRMED INEFFECTIVE by a third CI
    run: FFM.installDriver hung again at the identical spot in _shellExecuteElevatedAndWait
    (~89s, then manually canceled by the user - "I stopped it, it should only take < 10 seconds").
    Per the user's explicit fallback plan, pivoted to item 2 (PS1 cert bug) next instead of guessing
    further here. The registry tweak is left in the workflow (harmless either way, and may still be
    needed once the actual hang cause is found), but item 1 remains genuinely OPEN - the
    "unverified publisher" consent-dialog theory is still unproven, and this one candidate
    mitigation for it did not work.

FIX ATTEMPT 2 FOR ITEM 1 (commit 04c2ab1, 2026-08-30): once item 2 (below) was fixed and confirmed,
PS1.installDriver ran with a genuinely, validly-signed driver for the first time in this
investigation - and did NOT hang, elevating the exact same "cmd.exe /add-driver ..." command via
Start-Process -Verb RunAs (same underlying ShellExecuteEx mechanism FFM uses natively). That rules
out "PowerShell's elevation vs FFM's own" as the difference - both are the same Win32 API. What IS
different: FFM's _shellExecuteElevatedAndWait hardcodes SW_HIDE (nShow=0) for every elevated call,
including installDriver's cmd.exe target; PS1's Start-Process never passes -WindowStyle Hidden, so
its elevated cmd.exe window stays visible. FFM's other two elevated calls
(createAndTrustProvider/createAndSignCatalog) also use SW_HIDE, but they target javaw.exe - a
GUI-subsystem process with no console window to hide, so SW_HIDE is inert there and never surfaced
anything. installDriver's cmd.exe target is the only console-subsystem process FFM elevates with
SW_HIDE set, and the only one that hangs - a materially tighter correlation with the actual symptom
than the "unverified publisher" dialog theory (which required an interactive prompt that SW_HIDE
should have no bearing on either way).

_shellExecuteElevatedAndWait() now takes an optional nShow parameter (existing two call sites keep
SW_HIDE via a no-arg overload); installDriver()'s call now passes SW_SHOWMINNOACTIVE (7) instead -
creates/minimizes the window instead of suppressing it, without stealing focus on a real user's
desktop.

CONFIRMED INEFFECTIVE (commit 04c2ab1 reverted as d325711, 2026-08-30): a combined CI run (PS1
backend, then FFM backend, same job) appeared to show FFM.installDriver succeeding with this fix -
but it was a FALSE POSITIVE. PS1 had already staged the identical driver (same hardware ID) moments
earlier in that same run, so pnputil's fast dedup path fired for FFM too ("Driver package added
successfully. (Already exists in the system)") instead of the real install path - the fix was never
actually exercised. The user then dispatched FFM in isolation (fresh runner, nothing pre-staged) and
it hung exactly as before SW_SHOWMINNOACTIVE. Reverted; SW_HIDE was never the cause.

One thing this round DID newly establish: PS1 succeeds from a genuinely cold runner too (not merely
when run after FFM in the same job, and not merely benefiting from some earlier step) - so this hang
really is FFM-specific, not something both backends would hit under the right conditions. Both
candidate theories so far (unverified-publisher consent dialog, SW_HIDE on a console-subsystem
elevated process) are now disproven. Item 1 is back to OPEN with no working hypothesis. Next useful
step: get a thread dump / detailed diagnostic from an isolated FFM-only hang (the ~89s-before-manual-
cancel pattern from earlier runs only ever confirmed the hang is inside
_WaitForSingleObject.invoke(hProcess, ...) waiting on the elevated cmd.exe process handle itself -
i.e. the elevated child process genuinely never exits, not a bug in how this code waits for it).

FIX ATTEMPTED (commit 0eb40d4) - PS1.createAndSignCatalog: "Certificate not found" (CI run
90196100036), diagnosable for the
first time thanks to the try/catch added around the elevated script in the previous round:
    createAndTrustProvider succeeded (cert created + trusted, ~2 seconds before), but
    createAndSignCatalog's own separately-elevated child process's
    "Get-ChildItem Cert:\CurrentUser\My | Where-Object { $_.Subject -like '*CN=...*' }" found
    nothing and hit the "if(-not $cert) { throw 'Certificate not found' }" guard.

    Both elevated child scripts run via independent Start-Process -Verb RunAs calls from the same
    (non-elevated) outer PowerShell process, on a runner with silent admin elevation enabled
    (ConsentPromptBehaviorAdmin=0, set in the workflow) - in principle the same OS user/profile, so
    Cert:\CurrentUser\My (backed by that user's registry hive) should be identical across both
    elevated child processes. Why the second one didn't see the first one's cert is not yet
    understood.

    The "different elevation session -> different CurrentUser profile" candidate is now DISPROVEN:
    WindowsDriverInstaller_FFM does the structurally identical thing (two independently-elevated
    child processes, via _runElevatedSelf, each touching CERT_SYSTEM_STORE_CURRENT_USER - cert
    creation at ~line 844, lookup at ~line 1084) and it worked fine in this same CI run. Remaining
    candidates: whether New-SelfSignedCertificate's cert is actually written synchronously (i.e.
    fully committed to the store before -Wait returns), or a subject-string mismatch worth
    re-verifying byte-for-byte. A newer, unverified candidate specific to PS1: createAndSignCatalog
    passes its elevated script as a single -ArgumentList string
    ("-NoProfile -Command $script") that Start-Process must pass through raw Win32 argv parsing
    before the elevated powershell.exe re-tokenizes it - unlike FFM's approach, this could in
    principle corrupt embedded literal quotes in $script under the right conditions. Not checked.

    DIAGNOSTIC ADDED (commit 2bc4b01): the thrown "Certificate not found" message now includes a
    dump of Cert:\CurrentUser\My's actual contents (Subject, Thumbprint) at the point of failure, to
    distinguish an empty store from a subject-string mismatch. A live CI run confirmed via this
    diagnostic that the store was genuinely EMPTY ("Cert:\CurrentUser\My contains: []"), not a
    subject-string mismatch.

    FIX ATTEMPT 1 (commit 0eb40d4), CONFIRMED FAILED: the user identified that an earlier iteration
    of this backend, before -DeleteKey was added to its Remove-Item calls (commit 2a2cd6a, which
    added both the stale-cert cleanup at the start of createAndTrustProvider and the post-signing
    cleanup at the end of createAndSignCatalog), did not exhibit this "Certificate not found"
    symptom - -DeleteKey is the one PS1-specific change correlated with the regression's onset.
    Dropped -DeleteKey from both call sites (both now do a plain Remove-Item, leaving the stale
    certificate's CNG key container orphaned rather than destroyed).

    A subsequent live CI run proved this WRONG: with -DeleteKey removed, PS1.createAndTrustProvider
    still succeeded (cert created + trusted) and PS1.isProviderAlreadyTrusted (after trust) = [1],
    but PS1.createAndSignCatalog still failed with "Certificate not found. Cert:\CurrentUser\My
    contains: []" - the store was still completely empty. -DeleteKey was never the (sole) cause.
    User's reaction: "It does not make sense."

    The one useful thing this run's diagnostics DID confirm: Cert:\LocalMachine\Root/TrustedPublisher
    (machine-wide, not per-profile - checked by isProviderAlreadyTrusted) reliably survives across
    the two independently-elevated Start-Process -Verb RunAs sessions, while Cert:\CurrentUser\My
    (per-profile) does not, even though the first elevated child wrote to it successfully moments
    earlier. The underlying mechanism for why per-profile HKCU-backed stores don't carry over between
    separate elevations on this runner is still not understood, but the empirical split (LocalMachine
    survives, CurrentUser\My doesn't) is now solid across two independent CI runs.

    FIX ATTEMPT 2 (commit 5f03680): rather than keep guessing at the cross-session per-profile store
    behavior, stopped depending on it entirely. createAndTrustProvider() now, in addition to writing
    to Cert:\CurrentUser\My as before, exports the certificate + private key as a password-protected
    PFX to a %TEMP% file (`<providerName>.pfx`); the password is derived deterministically from
    providerName via SHA-256 (JxMake_WDI_PS1_PFX:<providerName>, base64 of the digest) so the two
    independent method calls can agree on it without persisting it anywhere. createAndSignCatalog()
    now loads the cert directly from that PFX via
    `[X509Certificate2]::new('<pfxPath>', '<password>')` instead of querying any Cert: store at all,
    and deletes the PFX in a `finally` block once done (createAndTrustProvider also eagerly deletes
    any stale leftover PFX from a prior call at its own start, in case createAndSignCatalog was never
    reached). This sidesteps the store-visibility mystery via the filesystem, which is not subject to
    whatever is breaking Cert:\CurrentUser\My here.

CONFIRMED FIXED (live CI, 2026-08-30): the PFX-handoff fix worked end-to-end.
createAndTrustProvider -> createAndSignCatalog (cert loaded straight from the PFX, catalog validly
signed, SignerCertificate/Status=Valid) -> installDriver all succeeded, pnputil reporting "Driver
package added successfully." Item 2 (this whole "Certificate not found" saga) is DONE.

BONUS DATA POINT FOR ITEM 1: PS1.installDriver, now handling a genuinely, validly self-signed
(non-WHQL) driver for the first time in this investigation, did NOT hang - it returned immediately.
This weakens the "pnputil raises an unverified-publisher consent dialog on a real-but-untrusted
signature, hanging headless" theory for FFM's still-open hang (item 1), since PS1 exercises the exact
same pnputil.exe /add-driver + non-WHQL-signed-catalog code path without hanging. FFM's hang cause is
now more likely something specific to FFM's own ShellExecuteExW/_shellExecuteElevatedAndWait
plumbing rather than a pnputil/UI behavior both backends would share. Item 1 remains OPEN; not yet
further investigated.

FOLLOW-UP (commit cbe4ab4) - restoring -DeleteKey now that it's safe: since createAndSignCatalog()
no longer reads Cert:\CurrentUser\My at all, the leftover cert+key createAndTrustProvider() writes
there served no further purpose once exported to the PFX - it just sat in the store indefinitely
with its private key intact. The user asked whether -DeleteKey should come back to (1) stop certs
accumulating in the store and (2) stop the key being reusable to sign anything else after one use.
Restored -DeleteKey, but only as a SAME-SESSION delete: destroy the Cert:\CurrentUser\My cert+key in
createAndTrustProvider's own elevated session immediately after the PFX export completes, plus in
its pre-existing stale-cleanup step at the top of the same method (defense in depth for a prior run
that crashed mid-way). This is a different code path from the earlier, reverted -DeleteKey removal
(fix attempt 1 above) - that regression was about a cross-session READ that never worked in the
first place; this is a same-session DELETE of an object the session just created, which was never
part of the broken mechanism.

RECONFIRMED (live CI, 2026-08-30, with cbe4ab4 included): same end-to-end success as the previous
run - createAndTrustProvider -> createAndSignCatalog -> installDriver all succeeded, pnputil
reporting "Driver package added successfully." The -DeleteKey restoration did not regress anything.
Item 2 is fully DONE.

DONE (2026-08-30, commit 927b5ed) - PS1 formatting cleanup: right-aligned every `\r\n" +` line
terminator within each multi-line String.format(...)/concat block in WindowsDriverInstaller_PS1.java.
Continuation backtick lines (New-SelfSignedCertificate, both Start-Process/-ArgumentList pairs, the
Set-AuthenticodeSignature pipe line) were left untouched. Confirmed
`grep -n '`[ \t]\+\\r\\n"' jxm/WindowsDriverInstaller_PS1.java` returns nothing, and both
`make jar JDK_VER=8` and `make jar JDK_VER=25` compile clean. Purely cosmetic; no behavior change.

ITEM 1 - FINAL DIAGNOSTIC AND DECISION TO DISABLE THIS BACKEND (2026-08-30):

    A third fix attempt for item 1 was made and disproven after the entries above were written: the
    user re-ran a fresh, FFM-only CI dispatch (no PS1 run in the same job, ruling out the test-
    ordering false positive that undermined the SW_SHOWMINNOACTIVE attempt) and captured a full Java
    thread dump while installDriver was hung (elapsed 157.80s before manual cancellation - "Error:
    The operation was canceled."). The dump shows the "main" thread in state RUNNABLE, blocked deep
    inside the native downcall stack (jdk.internal.foreign.abi.DowncallStub / LambdaForm$MH frames)
    at _shellExecuteElevatedAndWait (WindowsDriverInstaller_FFM.java:487), called from installDriver
    (WindowsDriverInstaller_FFM.java:394) - i.e. still stuck inside WaitForSingleObject, waiting on
    the elevated "cmd.exe /c pnputil.exe /add-driver ..." process handle itself.

    This is conclusive: the block is a genuine OS-level wait on the elevated child process handle -
    the elevated cmd.exe/pnputil.exe process itself never exits on this runner. It is NOT a bug in
    this codebase's Java-side logic, and is not explained by any of the three hypotheses tested so
    far (interactive unverified-publisher consent dialog, legacy Driver Signing\Policy registry gate,
    SW_HIDE vs SW_SHOWMINNOACTIVE). All three are now disproven; root cause remains unidentified.

    DECISION: given three independent, disproven fix attempts and a definitive thread dump that only
    narrows the problem down to "the OS-level process wait itself never completes" without suggesting
    a fourth hypothesis, the user decided to stop spending CI cycles on further guess-and-check
    debugging of this native, environment-specific hang, and to temporarily disable this backend
    entirely instead. WindowsDriverInstaller_FFM.isUsable() now unconditionally returns false (with a
    TODO comment pointing back to this section), so WindowsDriverInstaller.create() always falls back
    to WindowsDriverInstaller_PS1 - which independently reached full, CI-confirmed, end-to-end
    working status (see item 2's resolution above) and has no external SDK dependency. This
    "CONTINUE HERE NEXT SESSION" investigation log itself was moved out of WindowsDriverInstaller.java
    (previously lines 369+, after the class's closing brace) into this section, since the FFM
    investigation is now paused rather than actively continuing next session.

    Item 1 remains OPEN/UNRESOLVED, not fixed - it is set aside, not closed. If this backend is
    revisited in the future, useful next steps would include: reproducing the hang with Process
    Monitor or a similar tool attached to observe what the elevated cmd.exe/pnputil.exe process is
    actually doing (or waiting on) at the OS level while hung, since all three purely code-level
    hypotheses tried from the Java side have now been exhausted without success.

FFM RE-ENABLED (2026-08-31): item 2 (catalog signing) is resolved directly via SignerSignEx - see
SECTION E's "Investigation history, part 2" - so the external-tool detour is no longer needed.
WindowsDriverInstaller_FFM.isUsable() no longer hardcodes false. Item 1 (installDriver's elevated
pnputil.exe hang) is still OPEN/UNRESOLVED; installDriver() is a deliberate no-op for now while item 1
is set aside for a later session - see the no-op body's comment in WindowsDriverInstaller_FFM.java.

ITEM 1 - RESOLVED BY ALGORITHM CHANGE, NOT ROOT-CAUSE FIX (2026-08-31): installDriver() no longer
shells out to pnputil.exe at all, sidestepping the unresolved hang entirely rather than explaining
it. It now calls SetupCopyOEMInfW directly (guarded by CM_WaitNoPendingInstallEvents) inside the
same in-process elevated-JVM-relaunch this class already uses for createAndTrustProvider and
createAndSignCatalog - see SECTION H below. This is a meaningfully different code path from the old
one: _shellExecuteElevatedAndWait's WaitForSingleObject now waits on the elevated JVM child (already
proven not to hang, for the "trust"/"catalog" ops), never on cmd.exe/pnputil.exe. Not yet confirmed
on a live CI run.

FINAL RE-VERIFICATION (live CI, 2026-08-30, first run after the isUsable() disable landed):
    PS1: full end-to-end success once more - createAndTrustProvider -> createAndSignCatalog
    (SignerCertificate Status=Valid) -> installDriver ("Driver package added successfully") all
    succeeded, PS1.isProviderAlreadyTrusted correctly flipped from [0] to [1] after trust.
    FFM: FFM.isUsable() = false as expected, so it was skipped entirely with no attempt to run its
    (still-hanging) installDriver.
    create() factory pick: WindowsDriverInstaller_PS1, confirming the fallback works exactly as
    intended. This is the definitive confirmation that disabling FFM did not regress anything and
    that PS1 alone is a complete, working replacement for both backends' functionality.

test-windows-driver-installer.yml DISABLE STATUS: per the reminder comment at the top of the
workflow file itself (added when it was first written), once both backends passed a mutating run it
should be disabled (not deleted) via the Actions tab -> this workflow -> "..." -> Disable workflow,
so it stays available to re-enable for future regression testing (e.g. if FFM's hang is ever
revisited, or after further PS1 changes). That condition is now met - PS1 fully passes, FFM is
deliberately disabled rather than failing.

DONE (2026-08-30): `gh workflow disable "Test WindowsDriverInstaller"` initially failed with
`HTTP 403: Resource not accessible by personal access token` when run under this dev box's default
`GITHUB_TOKEN` environment variable (a fine-grained PAT that's sufficient for everything else used in
this investigation, e.g. `gh workflow list`, but not for this specific mutating Actions call). This
box also has a second `gh`-authenticated account in its credential keyring (an OAuth token with
`repo` scope), which `GITHUB_TOKEN` was silently overriding - `gh auth switch` alone doesn't help
here, since an explicit `GITHUB_TOKEN` env var always takes precedence over the active keyring
account. Running the same command with that env var unset for just that one call
(`env -u GITHUB_TOKEN gh workflow disable "Test WindowsDriverInstaller"`) used the keyring account
instead and succeeded. `gh workflow list --all` now shows it as `disabled_manually`. Fully reversible
via `gh workflow enable "Test WindowsDriverInstaller"` (same env-var caveat applies) or the Actions
tab's "..." -> "Enable workflow" - the file and its run history are untouched, only
`workflow_dispatch` is unavailable until it's re-enabled.

====================================================================================================
SECTION H - Driver Staging Layer (Setupapi.dll)
====================================================================================================

Replaces the old pnputil.exe shell-out (see SECTION G, "ITEM 1 - RESOLVED BY ALGORITHM CHANGE").
installDriver() dispatches to the elevated child exactly like createAndTrustProvider/
createAndSignCatalog (via _runElevatedSelf("install", ...)); the elevated child
(_elevatedInstallDriver) calls CM_WaitNoPendingInstallEvents as a guard, then
SetupSetNonInteractiveMode(TRUE), then SetupCopyOEMInfW to stage the INF. None of these functions
involve COM.

ITEM 2 - SetupCopyOEMInfW itself hung on live CI, even after ITEM 1's fix. Root-caused by manual
bisection (commenting out the actual `_SetupCopyOEMInfW.invoke()` call and hardcoding its return
value avoided the hang, while every other part of _elevatedInstallDriver - including the
CM_WaitNoPendingInstallEvents guard and the ShellExecuteExW-based elevation itself - did not hang
on its own): SetupCopyOEMInfW can internally attempt to show interactive UI (e.g. an
unverified-publisher/unsigned-driver confirmation dialog) in the caller's context. A headless CI
runner has no interactive desktop session and the elevated child's thread has no Windows message
pump, so such a dialog blocks forever - nothing can ever dismiss it. This is the same class of
failure as the original pnputil.exe hang (SECTION G), just against a different underlying Win32
call. Fix: call SetupSetNonInteractiveMode(TRUE) immediately before SetupCopyOEMInfW - see its
entry below. Not yet confirmed on live CI.

(SetThreadExecutionState(ES_CONTINUOUS|ES_SYSTEM_REQUIRED) was also considered as a fix, since it
was hit during the same bisection - rejected: it only prevents the *system* from entering sleep/
display-idle states via power-management timers, it has no effect on a thread blocked waiting for
UI/message-pump interaction, which is the actual mechanism here.)

ITEM 3 - after ITEM 2's fix, the hang was gone but SetupCopyOEMInfW returned BOOL FALSE with
GetLastError()=0 - a Win32 contract violation (a FALSE return must set a real, nonzero error).
Root cause: this file's usual `_lastError()` pattern (invoke the API, then invoke GetLastError()
as a SEPARATE, later downcall) is unreliable for a call that does real work between the two -
GetLastError() is Win32 thread-local storage, and the JVM can invoke its own Win32 APIs on that
same OS thread (e.g. during a GC/safepoint poll) in the gap between the two downcalls, silently
overwriting the value before this code reads it. This is a documented `java.lang.foreign` pitfall;
every OTHER `_lastError()` call site in this file is a short, fast Win32 call where that gap is
negligible, but SetupCopyOEMInfW does real file I/O to %windir%\Inf and is long enough to hit it in
practice (confirmed on live CI). Fixed by binding SetupCopyOEMInfW with
`Linker.Option.captureCallState("GetLastError")` instead, which atomically snapshots GetLastError
as part of the same downcall - see its FFM notes above and `_capturedLastError()` in the .java
file. Not yet confirmed on live CI whether this reveals a genuine, fixable underlying error or
whether SetupCopyOEMInfW itself still needs further investigation once the real code is known.

ITEM 4 - captureCallState (ITEM 3) revealed the real error: SetupCopyOEMInfW returned FALSE with
GetLastError()=0xE000024B = SPAPI_E_FILE_HASH_NOT_IN_CATALOG - the catalog was found and opened,
but the INF's hash wasn't accepted as matching a member in it, even though the hash itself was
computed correctly at catalog-build time (see CryptCATAdminCalcHashFromFileHandle2 above). Root
cause: the catalog member for the INF carried an "OSAttr" attribute (literal "10.0") intended to
scope it to Windows 10/11 - WindowsDriverInstaller_PS1's New-FileCatalog (proven working
end-to-end) never sets this attribute at all, and it turned out SetupCopyOEMInfW's validation
treats a member with an OSAttr it doesn't recognize/match as not-a-match at all, rather than just
narrowing which OS versions it applies to - i.e. it behaved as if the INF's hash wasn't in the
catalog. Fixed by dropping the "OSAttr" `CryptCATPutAttrInfo` call entirely, leaving only the
"File" attribute (matching PS1's approach) - see `CryptCATPutAttrInfo`'s FFM notes above.
CONFIRMED FAILED on the next live CI run: the exact same GetLastError()=0xE000024B recurred
verbatim, ruling the OSAttr hypothesis out.

ITEM 5 - After ITEM 4's fix was confirmed ineffective, a diagnostic log line was added at the hash
computation site to distinguish two remaining possibilities: a garbage/incorrectly-computed hash
(which would point at a `CryptCATAdminCalcHashFromFileHandle2` file-handle/read-position bug) vs a
correctly-computed hash that simply isn't what SetupCopyOEMInfW's own re-hash looks up. The next
live CI run showed `cbHash=32` and a genuine, non-trivial 64-hex-char digest (not the well-known
SHA-256-of-zero-bytes value) - ruling out a hashing bug. Root cause: the catalog only ever carried
a single SHA-256 member for the INF (`CryptCATAdminAcquireContext2` acquired with `"SHA256"`,
`szOID_NIST_sha256` in the SIP digest). SetupCopyOEMInfW's file-hash lookup was found to need a
SHA1 member specifically - real WHQL-issued driver catalogs carry both a SHA1 and a SHA2 `<HASH>`
entry for the same file for exactly this reason (older/legacy verification paths look up by SHA1;
newer tooling/OS components may look up by SHA2). Fixed by writing TWO full catalog members for the
INF - one per algorithm in `CATALOG_HASH_ALGS`/`CATALOG_HASH_ALG_OIDS` (`"SHA1"`/szOID_OIWSEC_sha1
and `"SHA256"`/szOID_NIST_sha256) - each with its own `CryptCATAdminAcquireContext2` context, hash,
SIP-indirect data, `CryptCATPutMemberInfo` call, and "File" attribute, all against the same open
`hCatalog`/`hInfFile`. SHA1 is kept alongside SHA256 (not swapped in place of it) since SHA1 alone
would be a regression for anything that specifically wants the SHA2 member; SHA256 alone was the
bug. Not yet confirmed on live CI.

----------------------------------------------------------------------------------------------------
SetupCopyOEMInfW - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/setupapi/nf-setupapi-setupcopyoeminfw

DLL          : Setupapi.dll
Header       : setupapi.h

Parameters   : SourceInfFileName (PCWSTR), OEMSourceMediaLocation (PCWSTR, optional),
               OEMSourceMediaType (DWORD), CopyStyle (DWORD), DestinationInfFileName (PWSTR,
               optional out buffer), DestinationInfFileNameSize (DWORD, in WCHARs),
               RequiredSize (PDWORD, optional out), DestinationInfFileNameComponent (PWSTR*,
               optional out - pointer into DestinationInfFileName where the filename part starts)
Return value : BOOL (nonzero on success; GetLastError() on failure)
Purpose      : Copies/stages an INF into %windir%\Inf (renamed to OEMnnnn.inf), making the driver
               package available to Windows without installing it onto any specific device.
               Requires administrative privileges. Does not require a physically-present device -
               this is the no-device-present path (this backend never calls
               UpdateDriverForPlugAndPlayDevices, which is the separate, device-present API).

FFM notes    : `FunctionDescriptor.of(DW, PTR, PTR, DW, DW, PTR, DW, PTR, PTR)` - 8 parameters,
               matches the prototype above 1:1. Bound against a `Setupapi.dll` SymbolLookup (see
               the static initializer), WITH `Linker.Option.captureCallState("GetLastError")` (see
               "ITEM 3" below for why) - this makes the downcall handle take an extra, PREPENDED
               `MemorySegment` argument (a capture-state buffer allocated per call via
               `arena.allocate(Linker.Option.captureStateLayout())`) ahead of the 8 real
               parameters. Called from `_elevatedInstallDriver` with OEMSourceMediaLocation=NULL
               and OEMSourceMediaType=SPOST_NONE (0) - infPath is already a fully-qualified local
               path passed as SourceInfFileName, so no separate media location applies.
               CopyStyle=0 (default: allow overwrite of an existing same-named staged copy).
               DestinationInfFileName is a caller-allocated MAX_PATH-WCHAR buffer, decoded back to
               a String for the elevated-child log on success (MemorySegment has no
               getUtf16String() helper, so this is done by hand: scan for the first NUL WCHAR,
               then StandardCharsets.UTF_16LE-decode the bytes up to it). RequiredSize/
               DestinationInfFileNameComponent are passed but not read.

CopyStyle    : 0 in the current call. SP_COPY_NOOVERWRITE (0x00000004) and SP_COPY_REPLACEONLY
               (0x00000002) are the two flags that would change this behavior; neither is needed
               since each CI run stages a freshly generated dummy INF and overwriting a prior
               same-named staged copy is the desired behavior, not an error case.

----------------------------------------------------------------------------------------------------
SetupSetNonInteractiveMode - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/setupapi/nf-setupapi-setupsetnoninteractivemode

DLL          : Setupapi.dll
Header       : setupapi.h

Parameters   : NonInteractiveFlag (BOOL)
Return value : BOOL - the PREVIOUS non-interactive-mode setting (not a success/failure indicator).
Purpose      : "Installation applications and co-installers can use this function to control
               whether SetupAPI can display interactive user interface elements, such as dialog
               boxes, in the caller's context." (Microsoft Learn remarks, verbatim). Companion
               getter SetupGetNonInteractiveMode exists but is not used here.

FFM notes    : `FunctionDescriptor.of(DW, DW)` - matches `BOOL(BOOL)` above 1:1. Bound against the
               same `Setupapi.dll` SymbolLookup as SetupCopyOEMInfW. Called from
               `_elevatedInstallDriver` with NonInteractiveFlag=1 (TRUE), immediately before
               SetupCopyOEMInfW - see SECTION H's "ITEM 2" note above for why this call exists.

----------------------------------------------------------------------------------------------------
CM_WaitNoPendingInstallEvents - VERIFIED
----------------------------------------------------------------------------------------------------
Source       : https://learn.microsoft.com/en-us/windows/win32/api/cfgmgr32/nf-cfgmgr32-cm_waitnopendinginstallevents

DLL          : Cfgmgr32.dll. Microsoft's own api_location metadata for this function also lists
               Setupapi.dll as an exporter, but a live CI run showed that does NOT hold at runtime -
               `SymbolLookup.find("CM_WaitNoPendingInstallEvents")` against a `Setupapi.dll` lookup
               failed (`isUsable()` returned false, with `FFM.initErrorDiagnostic()`'s
               `UnsatisfiedLinkError` pointing at this symbol), so it is bound against its own,
               separate `Cfgmgr32.dll` SymbolLookup instead - do not "simplify" this back to sharing
               the Setupapi.dll lookup without re-testing on real Windows CI.
Header       : cfgmgr32.h

Parameters   : dwTimeout (DWORD, milliseconds)
Return value : DWORD - nonzero (WAIT_OBJECT_0) if the wait was satisfied (no install activity
               pending), 0 (WAIT_TIMEOUT) if dwTimeout elapsed first, WAIT_FAILED on error.
Purpose      : Blocks until the PnP manager reports no device-install activity pending anywhere on
               the system, so a caller about to do its own install/staging work doesn't race a
               concurrent one already in progress.

Exported name: on 64-bit Windows, the documented name "CM_WaitNoPendingInstallEvents" is NOT the
               symbol Cfgmgr32.dll actually exports - a live CI UnsatisfiedLinkError against exactly
               that name confirmed it. The real x64 export is "CMP_WaitNoPendingInstallEvents" (one
               extra "P") - a well-known, long-standing quirk of how Cfgmgr32.dll's export table was
               built (its 32-bit build additionally exports a raw decorated name,
               "_CM_WaitNoPendingInstallEvents@8", which does not apply here since this backend is
               Windows-10+-only/x64-only). Same documented function and signature either way - this
               is a naming quirk in the DLL's export table, not an undocumented API.

FFM notes    : `FunctionDescriptor.of(DW, DW)` - matches `DWORD(DWORD)` above 1:1. Bound against a
               dedicated `Cfgmgr32.dll` SymbolLookup (see the "DLL" field above for why this isn't
               shared with SetupCopyOEMInfW's `Setupapi.dll` lookup), looking up the symbol
               "CMP_WaitNoPendingInstallEvents" (see "Exported name" above), not the documented
               "CM_WaitNoPendingInstallEvents". Called from `_elevatedInstallDriver` with
               dwTimeout=60000 (60s, not INFINITE) immediately before SetupCopyOEMInfW; a timeout
               here is logged but does not abort the subsequent SetupCopyOEMInfW call, since
               SetupCopyOEMInfW was never the call observed to hang in the old pnputil.exe-based hang
               investigation (see SECTION G).
