²Use debug prints and `make test` rather than relying only on static analysis to reduce quota usage
and prevent regressions on `(PASS)` tests and previous bug fixes.

To reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes, prefer
evidence over reasoning. Keep static analysis minimal—only enough to identify where to insert debug
prints. Use debug prints and `make test` to diagnose and validate fixes. Do not use static analysis
as the primary method of bug diagnosis or regression checking. After the fix is verified with
`make test`, remove all debug prints and then commit the files you have modified (ignore files you
have not modified). If unsure ask me.

####################################################################################################
####################################################################################################
####################################################################################################

Update and fix `CLAUDE.md`, `README.md`, `../README.txt`, `../AI_PREAMBLE_FULL.md`,
`../AI_PREAMBLE_AESTHETIC.md`, etc.
Ensure the languange order in the documentations, help strings, etc. is:
    c, cpp, java, kotlin, json, json5, css, yaml, toml, xml, html5, js, ts, python3
Adjust active vs scaffold-only as needed. Also ensure jsx and tsx are after js and ts.

I have run `make test` and all test passed.

Please clear old session data from `~/.claude`.

`/tmp` contains many undeleted artifacts from this and previous sessions, can you find the one used
for reproducing bugs and mini/small tests (not the cloned repositories) them and maybe delete them?



Compact (use one sub-agent) all newly touched `STATE_*.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) all `STATE_*.md` that were modified today.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_C_CPP_JAVA.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_CPP26.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_KOTLIN.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_DATA_FORMATS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_JS_TS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_PYTHON3.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_COMMON.md`, `STATE_KOTLIN.md`, and `STATE_AI.md`
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, the active work diagnosis, clustering, etc.
Then update `STATE_DOGFOOD.md` about the above dogfood state (keep `Note` short enough).

Compact (use one sub-agent) the narration in `test/README.txt`:
    from `real_code_regressions_173_inp/out.html`
    to   `real_code_regressions_176_inp/out.java`

Compact (don't use sub-agent because I will end this session soon) the narration in `test/README.txt`:
    from `...`
    to   `...`

----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ...
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Start with the dogfood test (in sequence) (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` and `....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Continue with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ... `...` (clusters ...-...)
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch. Commit after each batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.



C/C++:
    [X] `github.com/gcc-mirror/gcc`    [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
    [X] `github.com/llvm/llvm-project` [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
C++26:
Java:
    [X] `Cluster 5 (alignment-group padding collapse, `rewrite-kotlin/.../K.java`'s`
    [X] `**Extremely long pre-existing single-physical-line statement wraps differently each round**`
Kotlin:
JSON:
JSON5:
CSS:
YAML:
TOML:
XML:
HTML5:
JS:
    [X] `github.com/nodejs/node` [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
    [X] `**Content-preservation** (`js_ts_content_diff.js`, original vs. round1):`
TS:
Python3:

####################################################################################################
####################################################################################################
####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

if [ ! -d /sys/fs/cgroup/memory/claude_limit ]; then
    sudo cgcreate -t aloysius:aloysius -a aloysius:aloysius -g memory:claude_limit
    sudo bash -c "echo 4G > /sys/fs/cgroup/memory/claude_limit/memory.limit_in_bytes"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

cgexec -g memory:claude_limit ~/mynpm/bin/claude "$@"

####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

CG=/sys/fs/cgroup/claude_limit
if [ ! -d "$CG" ]; then
    sudo mkdir "$CG"
    sudo bash -c "echo '+memory' > /sys/fs/cgroup/cgroup.subtree_control"
    sudo bash -c "echo 4G > $CG/memory.max"
    sudo chown aloysius:aloysius "$CG" "$CG/cgroup.procs" "$CG/memory.max"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

echo $$ > "$CG/cgroup.procs"
exec  ~/mynpm/bin/claude "$@"

####################################################################################################
####################################################################################################
####################################################################################################

The GRU has new weights, trained using CM5 yesterday. The training log is `gru_log.txt`, do NOT
read the log in full, read ONLY THE LAST FEW LINES and then use `grep` for the other lines as
needed. Update the linear classifier weights to ensure they are up to date and calculate its %,
then compare it with the GRU % with the new weights. I have reduced the epoch and patience of the
GRU trainer in the `Makefile` because all the training so far always plateaued on epoch 3.

I have retrained the GRU. Check the GRU % with the new weights.
Update the `properties` - `AI-assist (GRU)` explanation in `README.md` and `STATE_COMMON.md`.

Continue with the `curly reindent job`.
Use one sub-agent. Ask the sub-agent to do it bit by bit and commit after each implementation
or bug fix. Pause after several implementation or bug fixes.

Continue with the `tc gap job`.
Use one sub-agent. Ask the sub-agent to do it bit by bit and commit after each implementation
or bug fix. Pause after several implementation or bug fixes.

Update `STATE_DOGFOOD.md` as needed.

####################################################################################################
####################################################################################################
####################################################################################################

=== TIER 0: Recurring job ===

1. GRU: **DO NOT REMOVE THIS ITEM. This will be a recurring job.**
   Further grow the hand-labeled hard-case corpus
   (`tools/classifier_weights/examples_*.md`).

   Rerun `classifiers/derive_weights.py` to update the linear classifier
   weights.

   Check the linear classifier % with the new weights.

   Rerun `make gru-acquire-corpus` so `sample_default.txt` will contain
   the new hand-labeled hard-case corpus.

2. GRU: **DO NOT REMOVE THIS ITEM. This will be a recurring job.**
   Retrain against the regenerated `sample_default.txt`

   Check the GRU % with the new weights.

3. GRU: **DO NOT REMOVE THIS ITEM. This will be a recurring job.**
   LLM-assisted disagreement sampling against `sample_default.txt`.

Use sub-agent. This can be a substantial work.

=== TIER 1: Analysis/docs/testing only — ~zero regression risk ===

1. [CPP26] Diff STYLE_CPP26.md vs STYLE_CPP20.md/STYLE_C_CPP.md to confirm
   which sections are genuinely C++26-specific vs already covered (§1-3
   suspected already covered; §4/§5 confirmed new). Last unchecked
   checklist item in that job.

2. [C_CPP_JAVA] Investigate reported clang C++23 vs gcc C++20 syntax-check
   mismatch on some passing fixtures — unconfirmed whether it's a real
   fixture/version issue or just stricter-parser noise.

3. [PYTHON3] Run the deferred full-corpus re-run (psf/black / django) now
   that all four known dogfood clusters are fixed.

4. [AI] Finish LLM-assisted disagreement-sampling steps 2-4 against
   sample_default.txt (design agreed 2026-07-30, not yet executed).

5. [COMMON] Do the cross-job housekeeping/cleanup pass (re-check "Known
   Gaps" for now-cheaper fixes, dead files, duplicated *Curly/*Indent/*Tags
   helpers, stale STATE_*.md notes) — scoped explicitly as housekeeping,
   not a rewrite.

Use sub-agent. This can be a substantial work.

=== TIER 2: Narrow/cosmetic fixes — low risk ===

1. [JS_TS] Fix single-declarator colon spacing bug: `const x: number = 1;`
   renders with a stray space before the colon (`const x : number`).

2. [AI] Confirm/wire `MiscRule.stripSoleTrailingPeriod`'s `dotCount != 1`
   case through the GRU classifier, then add mid-word-dot training rows
   (e.g. "Combined .hpp test ... extern C.") so sentence-ending periods
   aren't left in by mistake.

3. [DATA_FORMATS] `running.html` residual gap (RDD_KEY_223, discard-vs-
   synthesize `<p>`) — narrow, single known file, currently accepted.

4. [AI] GRU comment sentence-boundary mid-word-dot detection (`.hpp`, `e.g.`, `v1.0`
   defeating the sole-trailing-period heuristic) — accepted mechanical-rule
   limitation. Not just a training-data problem: first needs confirming/adding
   whether `MiscRule.stripSoleTrailingPeriod`'s `dotCount != 1` path even
   routes through the GRU yet (see STATE_AI.md's 2026-08-03 TODO note) — a real
   gate/wiring change, before any mid-word-dot training rows would matter.
   Value: low. Difficulty: medium (wiring confirmation/change first, then
   blocked on training data same as before).

Use sub-agent. This can be a substantial work.

=== TIER 3: Real feature gaps — medium risk ===

1. [DATA_FORMATS/XML] Implement XML known simplifications: text reflow,
   inline mixed text+element content, `indent-style = auto` detection,
   and §2.4's CDATA-inside-`<script>`/`<style>` dispatch exception.

2. [PYTHON3] Implement per-block indent-size/style conversion (Python
   analog of `MiscRuleCore.convertIndentation`) — block-boundary
   granularity still undecided, needs resolving against real drift
   patterns in psf/black/django fixtures first.

3. [C_CPP_JAVA] Fix non-idempotent switch-case re-indent on internally-
   inconsistent generated source (`SwitchRule.applyNonInlineCaseIndent`)
   — rare (1-2 files per large corpus so far), same root cause class as
   Tier 4 item 3 (curly-general-scope-reindent) below.

Use sub-agent. This can be a substantial work.

=== TIER 4: Architectural, cross-pass-ordering — high risk, prior attempts regressed fixtures ===

1. [JS_TS / C_CPP_JAVA, shared root cause] Fix call-wrap/collapse vs.
   declaration-alignment/padding fits-check ordering
   (`enforceCallLineBreaking` measuring width before other passes finish
   adjusting columns) — highest file-count impact seen (28/601 TS files,
   23/5394 angular files, plus a `SwitchRule`-adjacent case in
   lodash/lodash), user-deferred, no low-risk fix found yet.

2. [KOTLIN / CURLY_GDR, shared] Kotlin statement-boundary tracking for
   `MiscRuleCurly.renderCallCandidate`'s wrap-decision flap (D3) — two
   prior fix attempts each regressed fixtures (28, then 16); needs real
   statement/expression-boundary tracking, explicit go-ahead required
   before starting.

3. [CURLY_GDR] Broader adoption of `curly-general-scope-reindent` (still
   off by default) — multipass mode has cleared every corpus tested so
   far, but the "second-order oscillation" risk is only absent-so-far,
   not proven impossible; job's own instructions call this high risk.

4. JSX/TSX support — currently out of scope entirely (not merely deferred).

Use sub-agent. This can be a substantial work.

=== TIER X: Not worth pursuing / explicitly no action planned ===

- C/C++: `gcc-mirror/gcc`, `llvm/llvm-project` dogfood — NOT STARTED, too massive
  to even LOC-count, let alone dogfood in one pass.

- JS: `nodejs/node` dogfood — same, TOO MASSIVE.

- TS: Angular dogfood cluster 5 — accepted gap, no action planned (per
  STATE_JS_TS.md's own text).

- AI-assist Step 2 (argument-layout/getter-setter-grouping via LLM) — permanently
  NOT FEASIBLE, no tractable grouping-intent heuristic exists at any model size
  tested. Reference-only, not a live TODO.

####################################################################################################
####################################################################################################
####################################################################################################

You are an expert repository archivist working on JxMake's `formatter/` process
documentation: `CLAUDE.md`, `RDD_LOG.md`, and the `STATE_*.md` files
(EXCEPT `STATE_DOGFOOD.md`).

Read `CLAUDE.md` FIRST. It is the routing table: each `STATE_*.md` file belongs
to exactly one independent tracked job, and jobs are meant to be read/compacted
in isolation from each other — do not let context from one job's file leak
into, merge with, or get cross-referenced into another job's file. Preserve
that isolation in your output: one job's STATE file must still stand alone
after compaction, readable with no other STATE file open.

Process files one at a time, in this order: `CLAUDE.md`, then `STATE_COMMON.md`
(the shared process file every job depends on), then each remaining
`STATE_*.md` file individually. Do NOT combine any of them into a single
output. Do NOT touch `RDD_LOG.md` itself except as a read-only reference for
understanding what an `RDD_KEY_n` citation means — never inline, paraphrase,
or duplicate its content into a STATE file's own text.

CRITICAL RULES — violating any of these makes the output unusable:

1. **Never resolve or inline an `RDD_KEY_n` reference.** These are pointers
   into `RDD_LOG.md`, looked up one key at a time, on demand, by a future
   session — that is a deliberate token-budget design, not an oversight.
   Every `RDD_KEY_n` citation in the input must appear, verbatim (same key
   number, same surrounding one-line topic gloss), in the output. Do not
   summarize what a key "says" beyond the gloss already present in the
   input. If a paragraph names 3 keys, keep all 3 named — do not collapse
   them into "several design decisions were made."

2. **Never blur or resolve cross-job references.** If a STATE file mentions
   another job's file only as a pointer (e.g. "see STATE_JS_TS.md's Dogfood
   section"), keep it as a pointer. Do not pull in or summarize the other
   file's content to "help." Each job's file must remain readable in
   isolation, per `CLAUDE.md`'s explicit routing rule.

3. **Preserve every stated status distinction exactly**, especially these,
   which recur constantly and must never be flattened into each other:
   "done", "not started", "in progress", "accepted gap" / "known
   limitation" (deliberately left unfixed, not a bug), "deferred",
   "deviation from design" (differs from an earlier RDD decision, with a
   reason), "open question" (blocking, unresolved). A compaction that turns
   "known limitation, not yet fixed" into "fixed" or drops the "not yet"
   is a correctness bug in your output, not a style choice. When in doubt,
   quote the original status wording rather than paraphrase it.

4. **Do not launder aspirational or future-tense language into
   present-tense fact.** Several files are explicit that they describe
   only current, actually-landed code state — not documentation-writer
   intent, not what a doc *should* eventually say. If the input hedges
   ("no implementation yet", "design decisions landed but nothing shipped",
   "if this job proceeds"), your output must keep the same hedge. Never
   upgrade a design decision to "implemented" or a plan to "done."

5. **Never drop an explicit system/environment constraint.** Examples from
   this corpus: git flag incompatibilities, forbidden commands (e.g. "never
   run a filesystem-wide find", "never use `git worktree`" ,"never use
   `git stash`"), required wrapper scripts instead of calling a compiler/
   runtime directly, required grep discipline (e.g. "look up one RDD_KEY at
   a time, never add `-A` context"). These read like small process notes
   but are load-bearing — cutting one causes a future session to run an
   unsafe or broken command. Keep the constraint AND its stated reason if
   one is given.

6. **Keep every unresolved item completely intact, verbatim if needed:**
   open questions, active checklist items (checked and unchecked — do not
   drop completed items either, since a checklist's shape shows how much
   work led to the current state), current bugs, in-progress work, and any
   state variable a future session would need to resume work (e.g. current
   config defaults, active file:line references, fixture names actively
   registered in a Makefile/README).

7. **Config keys, defaults, and fixture/file names are data, not prose —
   never paraphrase them.** A key name, its default value, a file path, a
   fixture pair name, a commit hash, a function/class name: copy these
   character-for-character. Summarizing "various config keys were added"
   instead of naming them is a data-loss bug.

What you MAY condense, and how:
- Long historical narratives about HOW a now-resolved bug was found/fixed/
  investigated across multiple dogfood sessions may be shortened to: what
  was found, the root cause, the fix, and its current status — drop blow-
  by-blow investigation narrative once the outcome is stated. Keep any
  numeric result the input reports (e.g. "230/230 forward, 230/230
  idempotency") since these are gating evidence of "still green," not
  narrative color.
- Repeated/templated status log entries (e.g. a dogfood corpus re-run
  producing the same "clean, no new regressions" outcome across sessions)
  may be collapsed into one entry noting it was re-confirmed on the stated
  dates, rather than restating the full corpus-by-corpus breakdown each
  time — but keep the corpus names and file counts from at least the most
  recent run.
- Prose may be tightened for length as long as no rule above is violated.

Output format: one clean markdown file per input file, same filename,
written to an `out/` subdirectory next to the input files (e.g.
`out/STATE_HTML5_TCG.md`). No chat commentary, no preamble, no "here is the
compacted version" — just the file content itself. Preserve each file's
existing top-level heading structure (H1 title, `##` section headings) so a
human diffing old vs. new can still map sections 1:1.

After finishing all files, produce one extra file, `out/COMPACTION_REPORT.md`,
listing: original vs. new line/byte count per file, and a bullet list of
every RDD_KEY_n / status word / config key you were unsure whether to keep
verbatim vs. condense, so a human can spot-check exactly those spots first.

After I am reviewing them, and OK, I will ask you to overwrite the original files
and hit commit with your contribution and special e-mail in the commit message.
`Co-authored-by: Grok <grok@x.ai>`
