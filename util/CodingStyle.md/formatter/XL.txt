Use debug prints and `make test` rather than relying only on static analysis to reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes.

To reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes, prefer evidence over reasoning. Keep static analysis minimal—only enough to identify where to insert debug prints. Use debug prints and `make test` to diagnose and validate fixes. Do not use static analysis as the primary method of bug diagnosis or regression checking. After the fix is verified with `make test`, remove all debug prints and then commit the files you have modified (ignore files you have not modified). If unsure ask me.

Fix the above XXX bugs first, ignore the other parts of the diff for now.

####################################################################################################
####################################################################################################
####################################################################################################

Compact (use one sub-agent) `STATE_COMMON.md`, `STATE_C_CPP_JAVA.md` and `STATE_KOTLIN.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) the narration in `test/README.txt`:
    from `real_code_regressions_62_inp/out.kt`
    to   `real_code_regressions_67_inp/out.hpp`



I have run `make test` and all test passed.

`/tmp` contains many undeleted artifacts from this and previous sessions, can you find the one used
for reproducing bugs and mini/small tests (not the cloned repositories) them and maybe delete them?
Hmm do you think it is a good idea to delete them?

----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------

Continue with JSON/JSON5 work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.

----------------------------------------------------------------------------------------------------

Continue with CSS work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.

----------------------------------------------------------------------------------------------------

Continue with XML work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.

----------------------------------------------------------------------------------------------------

Continue with HTML work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.

----------------------------------------------------------------------------------------------------

Continue with JS/TS work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.

----------------------------------------------------------------------------------------------------

Continue with Python3 work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.

----------------------------------------------------------------------------------------------------

Continue with C++26 work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.

----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------

Continue with C/C++ work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Try reformat with `indent-size = 2` if the idempotency test failed.
    `github.com/microsoft/STL`     [~350 kLOC of effective C/C++ code] [NEXT - #1]
    `github.com/gcc-mirror/gcc`    [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
    `github.com/llvm/llvm-project` [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]

----------------------------------------------------------------------------------------------------

Continue with Java work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Use `java_sc` instead of `javac` if necessary. Try reformat with
`indent-size = 2` if the idempotency test failed.
    `../../../src` minus the already-DONE `../../../src/jxm` subtree    [NEXT - #0]
    `github.com/openrewrite/rewrite` [~700 kLOC of effective Java code] [NEXT - #2]

----------------------------------------------------------------------------------------------------

Continue with Kotlin work (search in /tmp first, clone the repo if not found; use one sub-agent,
the sub-agent can launch up to two more research sub-sub-agents):
...
When fixing bugs, only reformat and recompile the affected files; reformatting and recompiling
already correct files are a waste of time. Only after all bugs are fixed is a full reformat and
recompile test allowed. Use `kotlin_sc` instead of Gradle-copy dance. Try reformat with
`indent-size = 2` if the idempotency test failed.
    `github.com/JetBrains/kotlin` [~3000 kLOC of effective Kotlin code]

####################################################################################################
####################################################################################################
####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

if [ ! -d /sys/fs/cgroup/memory/claude_limit ]; then
    sudo cgcreate -t aloysius:aloysius -a aloysius:aloysius -g memory:claude_limit
    sudo bash -c "echo 4G > /sys/fs/cgroup/memory/claude_limit/memory.limit_in_bytes"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

cgexec -g memory:claude_limit ~/mynpm/bin/claude "$@"

####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

CG=/sys/fs/cgroup/claude_limit
if [ ! -d "$CG" ]; then
    sudo mkdir "$CG"
    sudo bash -c "echo '+memory' > /sys/fs/cgroup/cgroup.subtree_control"
    sudo bash -c "echo 4G > $CG/memory.max"
    sudo chown aloysius:aloysius "$CG" "$CG/cgroup.procs" "$CG/memory.max"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

echo $$ > "$CG/cgroup.procs"
exec  ~/mynpm/bin/claude "$@"
