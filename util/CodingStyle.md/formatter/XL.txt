Use debug prints and `make test` rather than relying only on static analysis to reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes.

To reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes, prefer evidence over reasoning. Keep static analysis minimal—only enough to identify where to insert debug prints. Use debug prints and `make test` to diagnose and validate fixes. Do not use static analysis as the primary method of bug diagnosis or regression checking. After the fix is verified with `make test`, remove all debug prints and then commit the files you have modified (ignore files you have not modified). If unsure ask me.

Fix the above XXX bugs first, ignore the other parts of the diff for now.

####################################################################################################
####################################################################################################
####################################################################################################

Compact (use one sub-agent) `STATE_DATA_FORMATS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_JS_TS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_CPP26.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_PYTHON3.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_COMMON.md`, `STATE_C_CPP_JAVA.md` and `STATE_KOTLIN.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) all newly touched `STATE_*.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) the narration in `test/README.txt`:
    from `real_code_regressions_125_inp/out.html`
    to   `...`

Compact (don't use sub-agent because I will end this session soon) the narration in `test/README.txt`:
    from `...`
    to   `...`



Update and fix `CLAUDE.md`, `README.md`, `../README.txt`, `../AI_PREAMBLE_FULL.md`,
`../AI_PREAMBLE_AESTHETIC.md`, etc.

Ensure the languange order in the documentations, help strings, etc. is:
    c, cpp, java, kotlin, json, json5, css, yaml, toml, xml, html5, js, ts, python3
Adjust active vs scaffold-only as needed. Also ensure jsx and tsx are after js and ts.



I have run `make test` and all test passed.

`/tmp` contains many undeleted artifacts from this and previous sessions, can you find the one used
for reproducing bugs and mini/small tests (not the cloned repositories) them and maybe delete them?

----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------

Continue with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ...
When fixing bugs, only reformat and re-syntax-checking the affected files; reformatting and
re-syntax-checking already correct files are a waste of time. Only after all bugs are fixed is
a full reformat and re-syntax-checking test allowed. Try reformat with `indent-size = 2` if the
idempotency test failed. Use one sub-agent as needed, the sub-agent can launch up to one more
research sub-sub-agent).

Continue with the dogfood test for:
    ...
If there are many errors, work in batch, store the rest in the corresponding state file.



C/C++:
    [X] `github.com/gcc-mirror/gcc`    [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
    [X] `github.com/llvm/llvm-project` [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]

C++26:

Java:
    [1] `github.com/apache/ant`          [~260 kLOC of effective Java code]
    [3] `github.com/openrewrite/rewrite` [~711 kLOC of effective Java code]
    [X] `**Extremely long pre-existing single-physical-line statement wraps differently each round**`

Kotlin:
    [7] `github.com/JetBrains/kotlin` [HUGE, ~3000 kLOC of effective Kotlin code]

JSON:
JSON5:
CSS:
YAML:
TOML:
XML:
HTML5:
    [*] `github.com/apache/ant`
    [X] `**TODO -- adoption agency, foster-parenting, ...`

JS:
    [X] `github.com/nodejs/node`          [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
    [X] `**TODO (low priority, not currently queued):** teach `js_ts_content_diff.js` ...`

TS:
    [4] `github.com/angular/angular`      [ ~895 kLOC of effective TypeScript code]
    [6] `github.com/microsoft/TypeScript` [~1490 kLOC of effective TypeScript code]

Python3:
    [2] `github.com/django/django`  [~455 kLOC of effective Python code]
    [5] `github.com/python/cpython` [~980 kLOC of effective Python code]



Continue with the dogfood test (in parallel; search in `/tmp` first, clone the repo if not found):
...
When fixing bugs, only reformat and re-syntax-checking the affected files; reformatting and
re-syntax-checking already correct files are a waste of time. Only after all bugs are fixed is
a full reformat and re-syntax-checking test allowed. Try reformat with `indent-size = 2` if the
idempotency test failed.



Read first and execute these in the most efficient order:

The tool name `*_content_diff*` does not match with tools names `*_sc.*`.
Either rename the former or the latter. Update the comments inside the source file too.

Some of the tool source code files may not have proper (C) block:
```
Copyright (C) 2022-2026 Aloysius Indrayanto

This file is part of the JxMake build system and is distributed under the Apache License, Version 2.0.
See the LICENSE file in the formatter root directory for the full Apache License, Version 2.0 text.
```
Add the block.

The dir name `syntax_checker` is not actually correct because it also hold content-diff tools.
Find a proper name for it and rename it.

Update `STATE*.md` to reflect the all renaming.

####################################################################################################
####################################################################################################
####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

if [ ! -d /sys/fs/cgroup/memory/claude_limit ]; then
    sudo cgcreate -t aloysius:aloysius -a aloysius:aloysius -g memory:claude_limit
    sudo bash -c "echo 4G > /sys/fs/cgroup/memory/claude_limit/memory.limit_in_bytes"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

cgexec -g memory:claude_limit ~/mynpm/bin/claude "$@"

####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

CG=/sys/fs/cgroup/claude_limit
if [ ! -d "$CG" ]; then
    sudo mkdir "$CG"
    sudo bash -c "echo '+memory' > /sys/fs/cgroup/cgroup.subtree_control"
    sudo bash -c "echo 4G > $CG/memory.max"
    sudo chown aloysius:aloysius "$CG" "$CG/cgroup.procs" "$CG/memory.max"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

echo $$ > "$CG/cgroup.procs"
exec  ~/mynpm/bin/claude "$@"
