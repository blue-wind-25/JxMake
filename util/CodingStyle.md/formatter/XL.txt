Use debug prints and `make test` rather than relying only on static analysis to reduce quota usage
and prevent regressions on `(PASS)` tests and previous bug fixes.

To reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes, prefer
evidence over reasoning. Keep static analysis minimal—only enough to identify where to insert debug
prints. Use debug prints and `make test` to diagnose and validate fixes. Do not use static analysis
as the primary method of bug diagnosis or regression checking. After the fix is verified with
`make test`, remove all debug prints and then commit the files you have modified (ignore files you
have not modified). If unsure ask me.

####################################################################################################
####################################################################################################
####################################################################################################

Ensure the languange order in the documentations, help strings, etc. is
    c, cpp, java, kotlin, json, json5, css, yaml, toml, xml, html5, js, ts, python3,
    makefile, bash, powershell
Adjust active vs scaffold-only as needed. Also ensure jsx and tsx are after js and ts.

I have run `make test` and all test passed.

Please clear old session data from `~/.claude`.

`/tmp` contains many undeleted artifacts from this and previous sessions, can you find the one used
for reproducing bugs and mini/small tests (not the cloned repositories) them and maybe delete them?

----------------------------------------------------------------------------------------------------

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ...
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Start with the dogfood test (in sequence) (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` and `....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Continue with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ... `...` (clusters ...-...)
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch. Commit after each batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.

----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------

Compact all `STATE_*.md` files. Use at most 5 sub-agents in parallel - balance the job between the
sub-agents based on the file sizes. Be careful not to lose important information, such as the tools/
compiler used, special formatter config (e.g., `indent-size`, etc.), exception/benchmark/helper
information, root cause, chain of reason, failed attempt at bug fix, etc. Basically, compact the
prose but keep the information intact.

Compact all `STATE_*.md` as needed. Use at most 4 sub-agents in parallel - balance the job.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) all `STATE_*.md` that were modified today.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_C_CPP_JAVA.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_CPP26.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_KOTLIN.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_DATA_FORMATS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_JS_TS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_PYTHON3.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_COMMON.md`, `STATE_KOTLIN.md`, and `STATE_AI.md`
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, the active work diagnosis, clustering, etc.
Then update `STATE_DOGFOOD.md` about the above dogfood state (keep `Note` short enough).

Update and fix `CLAUDE.md`, `DESIGN_NOTES.md`, `README.md`, `../README.txt`,
`../AI_PREAMBLE_FULL.md`, `../AI_PREAMBLE_AESTHETIC.md`, etc.

Compact (use one sub-agent) the narration in `test/README.txt`:
    from `real_code_regressions_203_inp/out.java`
    to   `real_code_regressions_203_inp/out.java`
[DONE]

Compact (don't use sub-agent because I will end this session soon) the narration in `test/README.txt`:
    from `...`
    to   `...`
[DONE]

####################################################################################################
####################################################################################################
####################################################################################################

=== TIER 0: Recurring job ===

1. [COMMON] **DO NOT REMOVE THIS ITEM. This will be a recurring job.**

   Open-ended "project refactoring/cleanup" sweep — unused boilerplate, duplicated
   `*Curly`/`*Indent`/`*Tags` helper logic. etc.

   Use sub-agent. This can be a substantial work.

2. [COMMON] **DO NOT REMOVE THIS ITEM. This will be a recurring job.**

   Apply the formatter to all `*.java/py/js/sh` source files inside `tools/*` subdirectories.
   Output the result to `/tmp/tools_round1` and then `/tmp/tools_round2`. Perform idempotency
   check  and content-diff check. If all OK, copy back the formatted round1 to the `tools/*`
   subdirectories.

   Apply the formatter to all the formatter's `*.java` source files inside `src/**/*.java`
   subdirectories. Output the result to `/tmp/fmt_round1` and then `/tmp/fmt_round2`. Perform
   idempotency check and content-diff check. If all OK, copy back the formatted round1 to the
   formatter's `src/**/` subdirectories.

   Update and fix `CLAUDE.md`, `STATE_DOGFOOD.md`, `README.md`, `../README.txt`,
   `../AI_PREAMBLE_FULL.md`, `../AI_PREAMBLE_AESTHETIC.md`, etc. as needed.

   Use sub-agent. This can be a substantial work.

3. [GRU] **DO NOT REMOVE THIS ITEM. This will be a recurring job.**

   !!! CLOSED FOR NOW - only revisit if much better training samples can be obtained !!!

   Further grow the hand-labeled hard-case corpus (`tools/classifier_weights/examples_*.md`),
   especially with the the `NO` samples.

   Rerun `classifiers/derive_weights.py` to update the linear classifier weights and check
   the linear classifier % with the new weights.

   Re-run `make gru-acquire-corpus` so `sample_default.txt` will contain the new hand-labeled
   hard-case corpus. I will retrain (run `make gru-train`) from another console later.

   Check the GRU % with the new weights.

   Run GRU cross validation and check the GRU % with the new weights.

   Use sub-agent. This can be a substantial work.

=== TIER 1: Analysis/refactor/docs/testing only — ~zero regression risk ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 2: Narrow/cosmetic fixes — low risk ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 3: Real feature gaps — medium risk ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 4: Architectural, cross-pass-ordering — high risk, prior attempts regressed fixtures ===

- [JS_TS] Boundary-finding pre-pass for regex/JSX-adjacent contexts — DESIGN LANDED 2026-08-12
  (STATE_JS_TS.md Open Questions, "2026-08-12 design session"): concrete expression-start-context
  list and opaque `JSX_SPAN` token representation now specified, plus the generic-vs-JSX ambiguity
  resolved by scoping the pre-pass to `.tsx`/`.jsx` files only (run before
  `reclassifyAngleBrackets`). Still NOT implemented — no code, no fixtures, this was a design-only
  pass. HIGH risk remains for the actual implementation (architectural, cross-pass-ordering); the
  context list is flagged as an unvalidated draft needing a real JSX corpus check before an
  implementation session trusts it as complete. It's the blocker for the future JSX/TSX
  embedding-aware dispatcher (STATE_JS_TS.md) — JSX/TSX itself is real planned future work per
  CLAUDE.md's job table, not a rejected idea, so this belongs here rather than TIER X: Dead (moved
  from there 2026-08-12).

Use sub-agent. This can be a substantial work.

=== TIER 8: Low priority ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 9: May not worth pursuing -- [Bug] ===

- [C_CPP_JAVA] `openrewrite/rewrite` 2 residual cosmetic idempotency diffs, new trigger sites of
  known bug families with no prior revert/regression history: switch-arrow braceless if/else
  closing-brace placement instability (`ModerneWebsiteExampleTest.java`, same shape as the
  cleanly-fixed Cluster 2); `List<String>` alignment-group padding collapses 3→1 space on round2
  (`Pep508RequirementTest.java`, same shape as the cleanly-fixed Cluster 5). (The third
  openrewrite diff — wrapped-call continuation indent growth — moved to Tier4 below: its own
  file text cites the `PowerShellSpecificRule.java` bug family, which had 3 reverted fix attempts.)

- [JS_TS] `hasBreakableCall` under-approximation ("known residual limitation"): the braceless-
  if/else collapse-rescue heuristic only checks whether a rescuable call exists, not whether
  wrapping it actually brings the line under `lineLengthLimit` — same "which pass sees final
  stable width" architectural class as RDD_KEY_245/246/248/249/250, one of which (RDD_KEY_249) had
  a reverted regression-causing fix attempt.

- [C_CPP_JAVA] `openrewrite/rewrite` wrapped-call continuation line gains 4 extra indent spaces on
  round2 (`TabsAndIndentsVisitor.java`, `YamlParser.java`) — file's own text cites this as the same
  "indent-width-decided-before-a-later-pass-grows-it" family as the `PowerShellSpecificRule.java`
  self-format bug, whose fix took 4 sessions/attempts with Attempts 1-3 each explicitly reverted
  for breaking 8 then 3 fixtures.

- [C_CPP_JAVA] Non-idempotent declarations-pass reindent on internally-inconsistent source, second
  occurrence (`ScopePipeline.applyDeclarationsPass`, `tool/JSONEncoderLite.java` repro) — same
  architectural "raw-source-indent vs. scope-depth-indent divergence" bug class as the switch-case
  gap that took 7 sessions to fix.

- [CURLY_GDR] Unverified risk: GDR's second multipass cycle (GDR-2) interacting with the pipeline's
  own relative-delta reindenters (`SwitchRule.applyNonInlineCaseIndent`,
  `ScopePipeline.applyDeclarationsPass`, the STYLE.md §8 continuation-indent renderer) — GDR-2
  could silently overwrite already-STYLE.md-compliant indentation those passes just wrote.
  Adversarial testing only covered a Java switch-in-if case; TS/Kotlin switch-case/ternary-wrap
  combinations are untried. Same bug family this job has already regressed on before. Note: unlike
  the other entries above, this is not yet a confirmed bug — it's an untested risk.

Use sub-agent. This can be a substantial work.

NOTE -- [2026-08-10] The 3 former entries here were removed as `README.md` Known Limitations entry:
- [C_CPP_JAVA] apache/ant `JikesOutputParser.java`.
- [TOOLING] ohmyzsh `tools/upgrade.sh` pipe-spacing.
- [KOTLIN] Kotlin D3 wrap-decision-flap statement-boundary tracking.

=== TIER 9: May not worth pursuing -- [Feature] ===

- [CURLY_GDR] `curly-general-scope-reindent = on` correctness still a "hard, multi-session
  problem" (continuation-vs-block depth, exclusion zones, ordering). HIGH risk (architectural,
  prior attempts regressed fixtures), high value (completes an already-shipped-but-default-off
  feature). Subsumes two narrower open facets already named elsewhere in `STATE_CURLY_GDR.md`:
  the `MAX_MULTIPASS_CYCLES = 20` convergence cap's untested adequacy on deeper/wider shapes, and
  `GdrTokenizer`'s general "acceptable at this stage" literal-shape-coverage caveat — neither
  needs its own separate line item.

- [C_CPP_JAVA] Retire `SwitchRule.applyNonInlineCaseIndent`/`ScopePipeline.applyDeclarationsPass`
  in favor of GDR pre-pass's absolute-target model — cleanup/consolidation, not a bug fix; medium
  risk (touches an already-working pass; regression risk is to correctness, not just style).

- [HTML5_TCG] Level-4 adoption-agency algorithm only tracks a single most-recently-orphaned
  formatting element; a second, simultaneous misnesting under the same ancestor is silently
  dropped instead of queued. Root-caused and confirmed against real `parse5` spec ground truth
  (2026-08-11) — spec-correct fix needs a fuller "reconstruct active formatting elements on next
  insert" mechanism, not a simple queue, which would expand level 4 toward the full spec algorithm
  the original design (RDD_KEY_230) deliberately rejected. Blocked pending a decision among 3
  options (leave as documented limitation / implement full mechanism / implement a documented
  non-spec-exact approximation) — see `STATE_HTML5_TCG.md`'s Open Questions. Not risk-rated like
  the entries around it — this is decision-pending (which of 3 approaches), not a go/no-go gamble.

- [DATA_FORMATS] XML general scope-depth reindentation ordering work. HIGH risk (same
  architecture class as CURLY_GDR above), lower value (XML rarely needs deep reindent compared
  to curly-brace nesting). Worst value/risk ratio in this tier — listed last accordingly.

Use sub-agent. This can be a substantial work.

=== TIER 9: May not worth pursuing -- [Dogfood] ===

- [C_CPP_JAVA] `gcc-mirror/gcc`, `llvm/llvm-project` dogfood — NOT STARTED, TOO MASSIVE to even
  LOC-count, let alone dogfood in one pass.

- [JS] `nodejs/node` dogfood — same, TOO MASSIVE.

Use sub-agent. This can be a substantial work.

=== TIER X: Dead ===

- [JS_TS] JSX/TSX support — currently out of scope entirely (not merely deferred).

- [PYTHON3] §5 f-string internal expression re-spacing (`f"{x  +  1}"` not normalized). CLOSED
  2026-08-12: matches `black`/`ruff format` convention (both also leave f-string interiors alone),
  so this is intentional scope, not a missed feature. Fixing it would need a real
  Python-expression tokenizer/spacer — reusing the C-family `needsSpaceBetween` risks silent
  semantic corruption (`*` multiplication, `**`/`//`/`:=`, keyword operators, comprehension
  `for`/`if`), not just a style miss. See `STATE_PYTHON3.md` §5.

- [AI] Dedicated GRU for non-Latin/mixed-language comments (currently always ABSTAIN via
  `NonLatinScriptGate`).

- [AI] Mid-word-dot sentence-boundary detection for trailing-period stripping
  (`MiscRuleCore.stripSoleTrailingPeriod`'s `dotCount != 1` case). CLOSED 2026-08-11: the
  task-separation path (second model/weights file, or a schema/task field on the shared
  abstain-resolution pipeline) is decided against permanently — cross-cutting correctness risk to
  the main GRU classifier's 92%+ precision outweighs closing this narrow gap. See `STATE_AI.md`.

- [AI] AI-assist Step 2 (argument-layout/getter-setter-grouping via LLM) — permanently
  NOT FEASIBLE, no tractable grouping-intent heuristic exists at any small model size tested.

####################################################################################################
####################################################################################################
####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

if [ ! -d /sys/fs/cgroup/memory/claude_limit ]; then
    sudo cgcreate -t aloysius:aloysius -a aloysius:aloysius -g memory:claude_limit
    sudo bash -c "echo 4G > /sys/fs/cgroup/memory/claude_limit/memory.limit_in_bytes"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

cgexec -g memory:claude_limit ~/mynpm/bin/claude "$@"

####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

CG=/sys/fs/cgroup/claude_limit
if [ ! -d "$CG" ]; then
    sudo mkdir "$CG"
    sudo bash -c "echo '+memory' > /sys/fs/cgroup/cgroup.subtree_control"
    sudo bash -c "echo 4G > $CG/memory.max"
    sudo chown aloysius:aloysius "$CG" "$CG/cgroup.procs" "$CG/memory.max"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

echo $$ > "$CG/cgroup.procs"
exec  ~/mynpm/bin/claude "$@"
