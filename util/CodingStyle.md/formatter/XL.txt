To reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes, prefer
evidence over reasoning. Keep static analysis minimal—only enough to identify where to insert debug
prints. Use debug prints and `make test` to diagnose and validate fixes. Do not use static analysis
as the primary method of bug diagnosis and regression checking. After the fix is verified with
`make test`, remove all debug prints and then commit the files you have modified (ignore files you
have not modified). If unsure ask me.

Ensure the languange order in the documentations, help strings, etc. is
    c, cpp, java, kotlin, json, json5, css, yaml, toml, xml, html5, js, ts, python3,
    eini, jxmake, makefile, bash, powershell
Adjust active vs scaffold-only as needed. Also ensure jsx and tsx are after js and ts.

I have run `make test` and all test passed.

Please clear old session data from `~/.claude`.

`/tmp` contains many undeleted artifacts from this and previous sessions, can you find the one used
for reproducing bugs and mini/small tests (not the cloned repositories) them and maybe delete them?

----------------------------------------------------------------------------------------------------

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ...
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Start with the dogfood test (in sequence) (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` and `....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Continue with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ... `...` (clusters ...-...)
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch. Commit after each batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.

----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------

Compact [all / all modified today] `STATE_*.md` files (NOT `RDD_LOG.md` -- it's the canonical
full-text store the STATE files already point to via `RDD_KEY_n`, don't summarize it).
Use at most 5 sub-agents in parallel - balance the job between the sub-agents based on the
file sizes. Each sub-agent touches only its assigned file(s) -- no cross-file/cross-job edits.
`STATE_COMMON.md` needs extra care: compact its narrative/history sections only; leave the
process rule text and the job table word-for-word, not paraphrased. Be careful not to lose important
information, such as the tools/compiler used, special formatter config (e.g., `indent-size`, etc.),
exception/benchmark/helper information, root cause, chain of reason, failed attempt at bug fix,
`RDD_KEY_n` references, etc. Basically, compact the prose but keep the information intact.
There is no size/percentage target -- judge by content (merge redundant narrative, cut
resolved back-and-forth, keep the final answer not the discussion that led to it); if a
section is already dense, leave it alone rather than cutting to hit a number. Commit each
file's compaction separately so a bad pass is easy to isolate/revert. Before and after each
file, run `grep -c 'RDD_KEY_' <file>` and confirm the count is unchanged. Since sub-agents run
in parallel with no coordination and some `RDD_KEY_n` chains span files, if a cross-file
wording/job-pointer inconsistency is noticed, leave it as-is and note it in the commit message
rather than editing the other file.

Compact (use one sub-agent) `STATE_COMMON.md`.
Compact (use one sub-agent) `STATE_C_CPP_JAVA.md`.
Compact (use one sub-agent) `STATE_CPP26.md`.
Compact (use one sub-agent) `STATE_KOTLIN.md`.
Compact (use one sub-agent) `STATE_DATA_FORMATS.md`.
Compact (use one sub-agent) `STATE_JS_TS.md`.
Compact (use one sub-agent) `STATE_PYTHON3.md`.
Compact (use one sub-agent) `STATE_TOOLING.md`.
Compact (use one sub-agent) `STATE_EINI.md`.
Compact (use one sub-agent) `STATE_JXMAKE.md`.
Compact (use one sub-agent) `STATE_AI.md`.
Compact (use one sub-agent) `STATE_CURLY_GDR.md`.
Compact (use one sub-agent) `STATE_HTML5_TCG.md`.
Compact (use one sub-agent) `STATE_DOGFOOD.md`.

Update and fix `CLAUDE.md`, `DESIGN_NOTES.md`, `README.md`, `../README.txt`,
`../AI_PREAMBLE_FULL.md`, `../AI_PREAMBLE_AESTHETIC.md`, etc.

Compact (use one sub-agent) the narration in `test/README.txt`:
    from `...`
    to   `...`
[DONE]

Compact (don't use sub-agent because I will end this session soon) the narration in `test/README.txt`:
    from `...`
    to   `...`
[DONE]

Use sub-agent. This can be a substantial work.

----------------------------------------------------------------------------------------------------

Try to implement the ... for:
...

If the new code causes too much regressions with `make test` (more than 20 or so lines), revert and
try another approach.

If it only causes few regressions, try to fix the regressions. If after ~3 tries and the regressions
is still not improved/somewhat fixed, revert and try another approach, otherwise continue for a bit.

If after ~3 approaches, the feature still cannot be properly implemented, then judge whether to
simplify the feature, divide the feature into smaller increments, or stop and cancel the feature
and add it to `## Known Limitations` in `README.md`.

If the feature is successfully implemented and re-dogfood is needed, only execute the full
re-dogfood once for each dogfood repo. In case there is further error in one/more of the dogfood
repos, fix the bug(s), and only reformat the failed files. Reformatting an entire dogfood repo
after each bug fix is wasting time.

Use sub-agent. This can be a substantial work.

####################################################################################################
####################################################################################################
####################################################################################################

=== TIER 0: Recurring job ===

1. [COMMON] **DO NOT REMOVE THIS ITEM. This will be a recurring job.**

    Process the formatter's Java source code and make improvements in this priority order:
     1. Refactor duplicated/common code.
     2. If a clear opportunity arises, optimize for execution speed.
     3. Perform other useful code clean-ups as needed.

    Prioritize the files that are modified after commit `f2e4842` but you can search for existing
    helper functions that already exist before that commit.

    Prioritize changes that are:
     - Small and focused.
     - Low-risk and behavior-preserving
     - Consistent with the existing code style and architecture.
     - Related to the stated tasks; avoid unrelated changes.

    For optimizations, do not make speculative changes. Prefer optimizations where the
    performance benefit is reasonably clear from the code.

    After several trivial changes, or after completing one substantial change, run `make test`.
    Fix any failures before continuing.

    Commit completed changes incrementally. Do NOT accumulate all changes and make one large
    commit. Each commit should contain a coherent set of related changes and have an appropriate
    commit message.

    Some of the code inside `tools/*` may use the formatter classes either directly or via its
    JAR. Ensure the refactoring and optimization process do not break the `tools`.

    You may launch up to three research sub-agents simultaneously.

    You may (but does not have to) launch only one code-writer sub-agent at a time.
    If a code-writing task is simple and mechanical, use Haiku instead of Sonnet.

    NEVER launch Opus or any more expensive sub-agent.

2. [COMMON] **DO NOT REMOVE THIS ITEM. This will be a recurring job.**

   Apply the formatter to all `*.java/py/js/sh` source files inside `tools/*` subdirectories.
   Output the result to `/tmp/tools_round1` and then `/tmp/tools_round2`. Perform idempotency
   check and content-diff check. If all OK, copy back the formatted round1 to the `tools/*`
   subdirectories. Perform some eyeballing as needed. Perform bug fixes as needed.

   Apply the formatter to all the formatter's `*.java` source files inside `src/**/*.java`
   subdirectories. Output the result to `/tmp/fmt_round1` and then `/tmp/fmt_round2`. Perform
   idempotency check and content-diff check. If all OK, copy back the formatted round1 to the
   formatter's `src/**/` subdirectories. Perform some eyeballing as needed. Running the test
   fixtures from the formatter JAR may produce regressions due to missing GRU weights. Perform
   bug fixes as needed.

   Find and fix accidentally flushed-left line and dedented lines because this may indicates
   formatter bug. Use `tools/detect_flushed_left_lines.py` (run with no args for usage) to scan
   for this instead of re-deriving the heuristic each time.

   Next, apply the formatter to:
       `../../JCS/*`
       `../../MDXplorer/*`
       `../../../3rd_party/tools/pcpp_java/*`
       `../../../3rd_party/tools/colordiff/colordiff.py`
   Output the result to `/tmp/something_round1` and then `/tmp/something_round2`. Perform
   idempotency check and content-diff check. If all OK, copy back the formatted round1 to the
   original subdirectories. Perform some eyeballing as needed. Perform bug fixes as needed.

   Update `CLAUDE.md`, `STATE_DOGFOOD.md`, `README.md`, `../README.txt`, `../AI_PREAMBLE_FULL.md`,
   `../AI_PREAMBLE_AESTHETIC.md`, etc. as needed.

   Use sub-agent. This can be a substantial work.

3. [GRU] **DO NOT REMOVE THIS ITEM. This will be a recurring job.**

   !!! CLOSED FOR NOW - only revisit if much better training samples can be obtained !!!

   Further grow the hand-labeled hard-case corpus (`tools/classifier_weights/examples_*.md`),
   especially with the the `NO` samples.

   Rerun `classifiers/derive_weights.py` to update the linear classifier weights and check
   the linear classifier % with the new weights.

   Re-run `make gru-acquire-corpus` so `sample_default.txt` will contain the new hand-labeled
   hard-case corpus. I will retrain (run `make gru-train`) from another console later.

   Check the GRU % with the new weights.

   Run GRU cross validation and check the GRU % with the new weights.

   Use sub-agent. This can be a substantial work.

=== TIER 1: Analysis/refactor/docs/testing only — ~zero regression risk ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 2: Narrow/cosmetic fixes — low risk ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 3: Real feature gaps — medium risk ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 4: Architectural, cross-pass-ordering — high risk, prior attempts regressed fixtures ===

NONE FOR NOW

Use sub-agent. This can be a substantial work.

=== TIER 8: Low priority ===

- line-split-by-operator-priority: declaration/condition collapse-on-round2
  flap, JS/TS manifestation fixed, original Java/guava manifestation still
  unreproduced (STATE_LINE_SPLIT_OP.md Known Out-of-Scope Finding).

Use sub-agent. This can be a substantial work.

=== TIER 9: May not worth pursuing -- [Bug] ===

- line-split-by-operator-priority: single-line function-body closing-brace
  placement, opening-brace half already fixed -- the straightforward fix
  (re-run scopePipeline.process()) was already tried and reverted twice for
  an analogous case, real regression risk, not just estimated difficulty
  (RDD_KEY_246/348, STATE_LINE_SPLIT_OP.md D10).

Use sub-agent. This can be a substantial work.

=== TIER 9: May not worth pursuing -- [Feature] ===

- line-split-by-operator-priority: Kotlin return/assignment-RHS split needs a
  real ASI-equivalent to work at all (method-chaining, multi-line if/when
  expressions, elvis continuation, trailing lambdas) -- large, separately-
  scoped subsystem, explicit corruption risk if done naively (RDD_KEY_347,
  STATE_LINE_SPLIT_OP.md Known Out-of-Scope Finding).

Use sub-agent. This can be a substantial work.

=== TIER 9: May not worth pursuing -- [Dogfood] ===

- [C_CPP_JAVA] `gcc-mirror/gcc`, `llvm/llvm-project` dogfood — NOT STARTED, TOO MASSIVE to even
  LOC-count, let alone dogfood in one pass.

- [JS] `nodejs/node` dogfood — NOT STARTED, TOO MASSIVE to even LOC-count, let alone dogfood in
  one pass.

Use sub-agent. This can be a substantial work.

=== TIER X: Dead ===

- [C_CPP_JAVA / JS_TS] `ScopePipelineCurly.processScope` double-pass rewrite. CLOSED — 4 sessions
  each fixed only a narrow instance, none scoped the rewrite itself. See `STATE_C_CPP_JAVA.md`,
  `RDD_KEY_330`.

- [JS_TS] JSX full embedding-aware dispatcher (general HTML5-tree-construction reuse +
  grammar-position `<` disambiguation). CLOSED — NOT FEASIBLE.

- [PYTHON3] f-string internal expression re-spacing (`f"{x  +  1}"` not normalized). CLOSED
  2026-08-12, intentional scope. See `STATE_PYTHON3.md` §5.

- [AI] Mid-word-dot sentence-boundary detection for trailing-period stripping. CLOSED
  2026-08-11. See `STATE_AI.md`.

- [AI] Dedicated GRU for non-Latin/mixed-language comments. Assessed, not planned. CLOSED
  2026-08-10. See `STATE_AI.md`.

- [AI] AI-assist Step 2 (argument-layout/getter-setter-grouping via LLM). CLOSED — permanently
  NOT FEASIBLE.

=== TIER Z: Closed, revisit only if new evidence surfaces ===

- [CURLY_GDR] GDR-2 (second multipass cycle) mis-scoping risk — NOT REPRODUCED. See
  `STATE_CURLY_GDR.md`.

- [CURLY_GDR] `GdrTokenizer` nested-block-comment gap — CLOSED, FIXED 2026-08-21. Kotlin block
  comments genuinely nest; `scanBlockComment` now counts nesting depth for Kotlin only (gated on
  language, C/C++/Java/JS/TS untouched). Fixture `real_code_regressions_227`. `make test`:
  339/339 → 340/340. See `RDD_KEY_333` in `RDD_LOG.md` / `STATE_CURLY_GDR.md`.

- [HTML5_TCG] Level-4 adoption-agency "two simultaneous orphaned formatting elements" —
  resolved 2026-08-15 (RDD_KEY_295), documented limitation. See `STATE_HTML5_TCG.md`'s
  Open Questions.

####################################################################################################
####################################################################################################
####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

if [ ! -d /sys/fs/cgroup/memory/claude_limit ]; then
    sudo cgcreate -t aloysius:aloysius -a aloysius:aloysius -g memory:claude_limit
    sudo bash -c "echo 4G > /sys/fs/cgroup/memory/claude_limit/memory.limit_in_bytes"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

cgexec -g memory:claude_limit ~/mynpm/bin/claude "$@"

####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"Rewriting

CG=/sys/fs/cgroup/claude_limit
if [ ! -d "$CG" ]; then
    sudo mkdir "$CG"
    sudo bash -c "echo '+memory' > /sys/fs/cgroup/cgroup.subtree_control"
    sudo bash -c "echo 4G > $CG/memory.max"
    sudo chown aloysius:aloysius "$CG" "$CG/cgroup.procs" "$CG/memory.max"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

echo $$ > "$CG/cgroup.procs"
exec  ~/mynpm/bin/claude "$@"

####################################################################################################

Launch a sub-agent to do this (ONLY for the files affected by this session's jobs -- see the GIT
commit log for this session as needed):

    Process the formatter's Java source code and make improvements in this priority order:
     1. Refactor duplicated/common code.
     2. If a clear opportunity arises, optimize for execution speed.
     3. Perform other useful code clean-ups as needed.

    Prioritize changes that are:
     - Small and focused.
     - Low-risk and behavior-preserving
     - Consistent with the existing code style and architecture.
     - Related to the stated tasks; avoid unrelated changes.

    For optimizations, do not make speculative changes. Prefer optimizations where the
    performance benefit is reasonably clear from the code.

    After several trivial changes, or after completing one substantial change, run `make test`.
    Fix any failures before continuing.

    Commit completed changes incrementally. Do NOT accumulate all changes and make one large
    commit. Each commit should contain a coherent set of related changes and have an appropriate
    commit message.

    Some of the code inside `tools/*` may use the formatter classes either directly or via its
    JAR. Ensure the refactoring and optimization process do not break the `tools`.

and then:

   Apply the formatter to all the formatter's `*.java` source files inside `src/**/*.java`
   subdirectories that WERE MODIFIED by the ABOVE job (readopt again).

   Output the result to `/tmp/fmt_round1` and then `/tmp/fmt_round2`. Perform idempotency check
   and content-diff check. If all OK, copy back the formatted round1 to the formatter's `src/**/`
   subdirectories. Perform some eyeballing as needed. Running the test fixtures from the formatter
   JAR may produce regressions due to missing GRU weights. Perform bug fixes as needed.

####################################################################################################
