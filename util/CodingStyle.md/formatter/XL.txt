²Use debug prints and `make test` rather than relying only on static analysis to reduce quota usage
and prevent regressions on `(PASS)` tests and previous bug fixes.

To reduce quota usage and prevent regressions on `(PASS)` tests and previous bug fixes, prefer
evidence over reasoning. Keep static analysis minimal—only enough to identify where to insert debug
prints. Use debug prints and `make test` to diagnose and validate fixes. Do not use static analysis
as the primary method of bug diagnosis or regression checking. After the fix is verified with
`make test`, remove all debug prints and then commit the files you have modified (ignore files you
have not modified). If unsure ask me.

####################################################################################################
####################################################################################################
####################################################################################################

Update and fix `CLAUDE.md`, `README.md`, `../README.txt`, `../AI_PREAMBLE_FULL.md`,
`../AI_PREAMBLE_AESTHETIC.md`, etc.
Ensure the languange order in the documentations, help strings, etc. is:
    c, cpp, java, kotlin, json, json5, css, yaml, toml, xml, html5, js, ts, python3
Adjust active vs scaffold-only as needed. Also ensure jsx and tsx are after js and ts.

I have run `make test` and all test passed.

Please clear old session data from `~/.claude`.

`/tmp` contains many undeleted artifacts from this and previous sessions, can you find the one used
for reproducing bugs and mini/small tests (not the cloned repositories) them and maybe delete them?



Compact (use one sub-agent) all newly touched `STATE_*.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) all `STATE_*.md` that were modified today.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_C_CPP_JAVA.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_CPP26.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_KOTLIN.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_DATA_FORMATS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_JS_TS.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_PYTHON3.md`.
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, etc.

Compact (use one sub-agent) `STATE_COMMON.md`, `STATE_KOTLIN.md`, and `STATE_AI.md`
Be careful not to lose important information, especially the `**Tools/compiler used**`,
`indent-size` exception, the active work diagnosis, clustering, etc.
Then update `STATE_DOGFOOD.md` about the above dogfood state (keep `Note` short enough).

Compact (use one sub-agent) the narration in `test/README.txt`:
    from `real_code_regressions_173_inp/out.html`
    to   `real_code_regressions_176_inp/out.java`

Compact (don't use sub-agent because I will end this session soon) the narration in `test/README.txt`:
    from `...`
    to   `...`

----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ...
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.

Start with the dogfood test (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Start with the dogfood test (in sequence) (search in `/tmp` first, clone the repo if not found):
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
   ... : ... (the repo is quite large, you may want to checkout the *.... files only)
Do NOT perform any bug fix yet, instead categorize and note the bugs inside
`....md` and `....md` into two categories:
    1. Critical bugs that cause formatter error/crash and output file corruption (syntax
       error, unwanted content change, etc.).
    2. Idempotency bugs only. Try reformat with `indent-size = 2` if the idempotency test
       failed.
For each category, cluster the bugs based on the types/appearance. Sort the clusters based on
the estimated criticality/the value of fixing the bug and the estimated difficulty (weigh them).
Sort the clusters from the most valuable to the least valuable to fix based on the above
criteria. Write and commit the state files, then ask me which clusters to fix first.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.
Write also your short report (which you print to the CLI console) to `/tmp/REP.txt`.

Continue with the dogfood test (search in `/tmp` first, clone the repo if not found):
    ... : ... `...` (clusters ...-...)
Try reformat with `indent-size = 2` if the idempotency test failed.
When fixing bugs, only reformat and re-syntax-checking the affected files to not waste of time.
Only after all bugs are fixed is a full reformat and re-syntax-checking test allowed.
If there are many errors, work in batch. Commit after each batch.
Use one sub-agent as needed, the sub-agent can launch up to one more research sub-sub-agent.



C/C++:
    [X] `github.com/gcc-mirror/gcc`    [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
    [X] `github.com/llvm/llvm-project` [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
C++26:
Java:
    [X] `**Cluster 5 — alignment-group padding collapse**: `rewrite-kotlin/.../K.java`, ...`
    [X] `**Extremely long pre-existing single-physical-line statement wraps differently each round**`
Kotlin:
JSON:
JSON5:
CSS:
YAML:
TOML:
XML:

HTML5:
    [X] `**Remaining TODO — adoption agency, foster-parenting, implicit ...`
    [X] `**Known gap found (2026-07-26, `apache/ant` `manual/` dogfood run):**`
JS:
    [X] `github.com/nodejs/node` [TOO MASSIVE - ghloc.vercel.app CANNOT EVEN COUNT THE LOC]
    [X] `**TODO (low priority):** teach `js_ts_content_diff.js` ...`
TS:
Python3:

####################################################################################################
####################################################################################################
####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

if [ ! -d /sys/fs/cgroup/memory/claude_limit ]; then
    sudo cgcreate -t aloysius:aloysius -a aloysius:aloysius -g memory:claude_limit
    sudo bash -c "echo 4G > /sys/fs/cgroup/memory/claude_limit/memory.limit_in_bytes"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

cgexec -g memory:claude_limit ~/mynpm/bin/claude "$@"

####################################################################################################

#!/bin/bash

export NODE_PATH=/opt/node-v24.14.0-linux-x64/lib/node_modules
export PATH=/opt/node-v24.14.0-linux-x64/bin:$PATH

npm config set prefix ~/mynpm

export NODE_OPTIONS="--max-old-space-size=2048"

CG=/sys/fs/cgroup/claude_limit
if [ ! -d "$CG" ]; then
    sudo mkdir "$CG"
    sudo bash -c "echo '+memory' > /sys/fs/cgroup/cgroup.subtree_control"
    sudo bash -c "echo 4G > $CG/memory.max"
    sudo chown aloysius:aloysius "$CG" "$CG/cgroup.procs" "$CG/memory.max"
fi

trap 'kill -- -$$ 2>/dev/null; wait' EXIT

echo $$ > "$CG/cgroup.procs"
exec  ~/mynpm/bin/claude "$@"

####################################################################################################
####################################################################################################
####################################################################################################

Launch a sub-agent for:

    GruTrainer speed: profile the per-example forward/backward hot path
    (computeBatch/computeGradients) and refactor to flat, JIT-auto-
    vectorizable double[]/float[] loops (no aliasing/branches in the hot
    loop) ...

    Limit the job so it does not consume too many times and usage quota.
    Prepare a small but enough training data. Profile the original trainer
    code. Make the changes to one-two functions, profile again. Change
    more functions, profile again, and so on. If the new functions have the
    same/similar performance than the old, prefer the raw array `[]`.
    Do NOT profile with combination of changes, unless absolutely required.
    If need to choose between `double` and `float`, use whatever the current
    code use first (should be `double`). Try `float` only if all conversion
    to raw array `[]` have been completed, however, do not try `float` if
    you predicted that the precision will not be enough.

Let's discuss:

(1a)
When `General scope-depth reindentation` is implemented, it will reindent
code that on-purpose made to have different indentations such as:
            if(...) {
                ...
                ...
   if(_A_FLAG) {
       ...
    }
                ...
                ...
            }
This means either something like:
    //% JXM_CFMT_GDR 0
    //% JXM_CFMT_GDR 1
or:
    //% JXM_CFMT_CFG indent-size=N
must be relaxed so it can appear in the middle of code. The 1st approach
might be better.

(1b)
To make `General scope-depth reindentation` safe, how if it is implemented
as if C preprocessor.

If enabled via (the default is off):
    enable-curly-gdr on
Then a prepass with its own simpler tokenizer, depth counter, and reindenter
will be applied to the source code before then sent to the current formatter
flow. This way the current formatter code/flow is not modified and there will
be no regression, at least when the above config is not enabled.

(1c)
Does the job `JetBrains/kotlin` dogfood cluster D3 really needs to be folded
to `General scope-depth reindentation`? If so, fold the job.

Write the result of this (1x) discussions to the proper state files with the
instructions to update `README.md`, state files, erc. when this feature is
already implemented. Ensure the instruction to also include a task to generate
new test fixtures for this (use `JXM_CFMT_CFG` to turn on `enable-curly-gdr`
from the in-file config). Update also the `Tier 3` items in XL.txt but do not
commit XL.txt.

-----

(2) The current fornatter format if-else to something like:

if(...) xxx;
else    yyy;

if(...)              xxx;
else if(...)         yyy;
else if(... ... ...) zzz;
else                 qqq;
 
How difficult it is to make the second case into:

     if(...)         xxx;
else if(...)         yyy;
else if(... ... ...) zzz;
else                 qqq;

If it is not difficult, launch a sub-agent to implement it for all
curly languages. Then to update the affected `*_out.*` fixtures, and
`SYTLE*.md`, `AI_PREAMBLE_*.md`, `../README.txt`, etc. If it deemed
to be difficult, put as a TODO in `Tier 1` items in XL.txt but do not
commit XL.txt.



--- Tier 1: high value, tractable next steps ---

10. GRU: retrain against the regenerated `sample_default.txt` (post
    extract_comments.py fix + post hard-case corpus growth) and re-run the
    hard-case benchmark. Tooling already built end to end (`make gru-train` +
    `GruEval`), see STATE_AI.md's most recent sessions for exact current
    numbers/status. Value: medium (directly tests whether the corpus fixes
    moved the GRU closer to the linear-classifier baseline). Difficulty: low.

11. GruTrainer speed: profile the per-example forward/backward hot path
    (computeBatch/computeGradients) and refactor to flat, JIT-auto-
    vectorizable double[]/float[] loops (no aliasing/branches in the hot
    loop) — plain-Java win, no toolchain change, orthogonal to the existing
    --threads parallelism. Considered but explicitly deferred:
    jdk.incubator.vector (Vector API) — still incubating even on JDK 25, API
    may still change, so not worth adopting yet; hand-rolled FFM-generated
    machine code was also considered and rejected as high-effort/high-risk
    for no clear gain over the Vector API path once that stabilizes. Value:
    medium (training turnaround, esp. for the planned CM5 run). Difficulty:
    low-medium (profiling + refactor + `make test`/gradient-check
    verification, not a redesign).

Use sub-agent. This can be a substantial work.

--- Tier 2: meaningful, narrower value or higher difficulty ---

20. GRU: **DO NOT REMOVE THIS ITEM. This will be a recurring job.**
    further grow the hand-labeled hard-case corpus
    (`tools/classifier_weights/examples_*.md`) — repeatedly identified in
    STATE_AI.md as the highest-leverage lever for closing the GRU/linear-
    classifier precision gap, more so than trainer mechanics. Value:
    medium-high. Difficulty: low-medium (careful hand-labeling, no code).

    Rerun `classifiers/derive_weights.py` to update the linear classifier
    weights.

    Rerun `make gru-acquire-corpus` so `sample_default.txt` will contains
    the new hand-labeled hard-case corpus.

    DO NOT REMOVE THIS ITEM. This will be a recurring job.

21. GRU: LLM-assisted disagreement sampling against `sample_default.txt`
    Value: low-medium (corpus-quality improvement/further evidence-gathering, most
    of the high-value finding already extracted). Difficulty: low (reading/judging
    text, no code).

Use sub-agent. This can be a substantial work.

--- Tier 3: big/risky architectural bets ---

30. `JetBrains/kotlin` dogfood cluster D3 — multi-line-call/condition
    wrap-decision flap (`MiscRuleCurly.renderCallCandidate`'s fits-check
    measures against the whole enclosing physical line instead of a stable
    per-candidate position). Value: medium (single repo). Difficulty: high —
    two separate anchor designs have each been tried and cleanly reverted; the
    real fix needs actual statement-boundary tracking, same shape of problem
    as item 31 below, not a self-contained surgical fix. Do not re-attempt with
    another narrow anchor tweak; either fold into a combined design pass with
    item 31, or leave parked until item 31 is seriously scoped. Full history
    and root-cause detail: STATE_KOTLIN.md's "D3 implementation attempt"
    sections, RDD_KEY_226.

31. General scope-depth reindentation (STATE_COMMON.md "Architectural TODOs",
    cross-cutting — not owned by one job). Would close two ACCEPTED-not-fixed gaps
    (`javaparser/javaparser`'s `ASTParser.java`, local `JSONEncoderLite.java`) and
    any future bug of the same shape. Value: potentially high (removes a whole bug
    class) but the file itself flags this as high-risk: blast-radius inversion
    (every line in every file becomes a candidate for a wrong result, vs. today's
    ~1/2000-file exposure), a second continuation-vs-block-depth axis to merge,
    exclusion zones (raw strings, comments, preprocessor, `frozen` spans) that
    multiply, and ordering interactions with every other pass. Difficulty: very
    high. Do not attempt without a dedicated design pass first.

32. JSX/TSX support — currently out of scope entirely (not merely deferred),
    pending a future embedding-aware dispatcher (STATE_JS_TS.md). Value: high (real
    coverage gap, analogous to HTML5's `<script>` dispatch but harder). Difficulty:
    high — new dispatch architecture, not an incremental rule.

33. HTML5 deep tree-construction gaps (foster-parenting, case-folding, adoption
    agency algorithm) — only bites `web-platform-tests/wpt`'s deliberately
    pathological conformance fixtures, not real-world content (STATE_DATA_FORMATS.md
    confirms no real WPT syntax-page is affected). Value: low. Difficulty: high —
    full tree-construction algorithm work for edge cases that don't occur in
    practice.

Use sub-agent. This can be a substantial work.

--- Tier 4: ML/infra nice-to-haves — defer until a real training/tuning cycle needs them ---

40. GruTrainer: automatic abstain-threshold tuning (needs a genuine held-out
    labeled validation slice, not yet built, + a chosen objective — see
    STATE_AI.md for why the existing hard-case set isn't yet suitable).
    Value: low-medium. Difficulty: medium. Update `gru/README.txt` too.
41. GruTrainer: dropout before dense layer — regularization only; STATE_AI.md's
    diagnosis is this isn't the bottleneck (a data-representation problem, not
    an optimization one) so value is unclear until that's re-tested. Value:
    low. Difficulty: medium. Update `gru/README.txt` too.
42. GRU: comment sentence-boundary mid-word-dot detection (`.hpp`, `e.g.`, `v1.0`
    defeating the sole-trailing-period heuristic) — accepted mechanical-rule
    limitation, feasible only once the GRU has enough mid-word-dot training
    examples; not independently actionable right now. Value: low. Difficulty:
    blocked on more training data, not really "difficulty" in the usual sense.

Use sub-agent. This can be a substantial work.

--- Others ---

`make gru-train`
Continue with GRU work: recheck GRU performance with new weights.

GRU: further grow the hand-labeled hard-case corpus.
Rerun `classifiers/derive_weights.py` to update the linear classifier weights.
Rerun `make gru-acquire-corpus` so `sample_default.txt` will contains the new hand-labeled hard-case corpus.

Still waiting for GRU-training to complete, please implement:
    ...
Use sub-agent. This can be a substantial work.

--- Not worth pursuing / explicitly no action planned ---

- C/C++: `gcc-mirror/gcc`, `llvm/llvm-project` dogfood — NOT STARTED, too massive
  to even LOC-count, let alone dogfood in one pass.
- JS: `nodejs/node` dogfood — same, TOO MASSIVE.
- TS: Angular dogfood cluster 5 — accepted gap, no action planned (per
  STATE_JS_TS.md's own text).
- AI-assist Step 2 (argument-layout/getter-setter-grouping via LLM) — permanently
  NOT FEASIBLE, no tractable grouping-intent heuristic exists at any model size
  tested. Reference-only, not a live TODO.
