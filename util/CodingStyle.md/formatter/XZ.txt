Let's simplify this job before executing it:

    GruTrainer speed: profile the per-example forward/backward hot path
    (computeBatch/computeGradients) and refactor to flat, JIT-auto-
    vectorizable double[]/float[] loops (no aliasing/branches in the hot
    loop) ...

Basically, raw arrays `[]` would not be slower than the more sophisticated
data types. Therefore
- Prepare a small but enough training data.
- Benchmark the original trainer code using the above data.
- Convert all to use raw arrays `[]` and refactor to vectorizable loop.
- Benchmark again.
- Commit all GRU related code.

If you think `float` is enough, then convert all to `float`, including the
code for inference and JSON data. There is no point to train using `float`
and then storing the JSON and running the inference using `double`. If you
decide to convert to `float`:
- Ensure all the GRU related code both for trainer and inference are committed.
- Run the GRU and check with the existing weights to check its % correctness.
- Convert the inference code to `float` (you will need to make it to be able
  to read JSON `double` w to `float` variables for the weights). Then  compare
  the % correctness after converting, if it becomes bad, revert and keep using
  `double`.
- If the % after conversion is still good/similar, then also convert the trainer
. to use `float` and perform the benchmark again.
- Commit both the inference and trainer code.

Do NOT retrain with the real data, I will do this later. Therefore you will
want to make the JSON loader in the inference code to be able to load both
`double` and `float` strings from JSON.

Use sub-agent for the actual job execution.

-----

Let's discuss:

(1a)
When `General scope-depth reindentation` is implemented, it will reindent
code that on-purpose made to have different indentations such as:
```
            if(...) {
                ...
                ...
   if(_A_FLAG) {
       ...
    }
                ...
                ...
            }
```
This means something like:
```
//% JXM_CFMT_GDR 0
//% JXM_CFMT_GDR 1
```
is needed (along with the `/* */` variant).

(1b)
To make `General scope-depth reindentation` safe, how if it is implemented
as if C preprocessor?

If enabled via (the default is off):
```
enable-curly-gdr on
```
Then a pre-pass with its own simpler tokenizer, depth counter, and reindenter
will be applied to the source code before then sent to the current formatter
flow.

This way the current formatter code/flow is not modified and there will be no
regression, at least when the above config is not enabled.

(1c)
Does the job `JetBrains/kotlin` dogfood cluster D3 really needs to be folded
to `General scope-depth reindentation`? If so, fold the job and update
`STATE_KOTLIN.md` as needed.

Based on the result of this (1a-c) discussions, create `STATE_CURLY_GDR.md`
that contains:
- The updated text from `General scope-depth reindentation (not started — high risk, read before attempting)`
. from `STATE_COMMON.md`. Remove the old text from `STATE_COMMON.md` after that.
- The result of the above discussion.
- Instructions to update `README.md`, when this feature is finally implemented.
- Instruction to generate one or two new test fixtures for this (use
..`JXM_CFMT_CFG` to turn on `enable-curly-gdr` from the in-file config). The
 nselected language should not matter, but put them after `in_file_config_error_inp.hpp`
in `Makefile` and under `in_file_config_error_inp/out.hpp` in `test/README.txt`.

Like the other state files, `STATE_CURLY_GDR.md` must:
- Reference `STATE_COMMON.md`.
- Gated from `CLAUDE.md`, marked as `curly reindent` job.
However, `STATE_COMMON.md` must NOT reference `STATE_CURLY_GDR.md`.

Then fill-in the checklist and scoping in `STATE_CURLY_GDR.md`.

Use sub-agent for the actual job execution.

-----

(2) The current formatter format if-else to something like:

if(...) xxx;
else    yyy;

if(...)              xxx;
else if(...)         yyy;
else if(... ... ...) zzz;
else                 qqq;
 
How difficult it is to make the second case into:

     if(...)         xxx;
else if(...)         yyy;
else if(... ... ...) zzz;
else                 qqq;

If it deemed to be difficult, put it as a TODO in
`Tier 1` items in `XL.txt` after `11. GruTrainer speed`,
but do not commit `XL.txt`.

If it is not difficult, launch a sub-agent to:
- Implement it for all curly languages (they all should
  already share the code).
- Then to update the affected `*_out.*` fixtures, and
  `SYTLE*.md`, `AI_PREAMBLE_*.md`, `../README.txt`, etc. 

You may want to wait for the previous sub-agent to complete,
because the previous sub-agent will also modify the MD files.
