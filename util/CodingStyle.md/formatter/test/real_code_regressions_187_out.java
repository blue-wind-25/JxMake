/*
 * Copyright (C) 2024 Example Corp.
 * SPDX-License-Identifier: MIT
 */
class Example {

    void parsePackage()
    {
        if( a == null && b.equals(c) && d.equals(e) ) {
            int saveCursor = cursor;
        }
        if(flag) {
            boolean ignored2 = computeIt();
        }
    }

} // class Example
