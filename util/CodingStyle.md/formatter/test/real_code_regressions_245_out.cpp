/*
 * Copyright (C) 2024 Example Corp.
 * SPDX-License-Identifier: MIT
 */

void f()
{
    // stat() call per entry compared to os.listdir() + os.path.isdir()

    // stat() call per entry compared to os.listdir() + os.path.isdir(). After that do this and that.

    // stat() call per entry compared to os.listdir() + os.path.isdir().
    // After that do this and that.


    /* stat() call per entry compared to os.listdir() + os.path.isdir() */

    /* stat() call per entry compared to os.listdir() + os.path.isdir(). After that do this and that. */

    /* stat() call per entry compared to os.listdir() + os.path.isdir().
       After that do this and that. */
}
